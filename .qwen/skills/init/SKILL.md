---
name: init
description: "First-time project setup. Creates .tausik/tausik.db and initializes project structure. Use when user says 'init', 'initialize', 'setup project'."
context: inline
effort: fast
source: official
---

# /init — on-demand skill

This skill is available but not loaded into context yet.
Run `.tausik/tausik skill activate init` or use `tausik_skill_activate` MCP tool to load the full version.

Tags: team
