---
name: dispatch
description: "Orchestrate parallel worker agents. Use when user says 'dispatch', 'parallelize', 'run workers', 'multi-agent'. Args: [task-slugs|story-slug|epic-slug]"
context: fork
effort: slow
source: official
---

# /dispatch — on-demand skill

This skill is available but not loaded into context yet.
Run `.tausik/tausik skill activate dispatch` or use `tausik_skill_activate` MCP tool to load the full version.

Tags: workflow
