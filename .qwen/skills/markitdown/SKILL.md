---
name: markitdown
description: "Convert DOCX / PPTX / XLSX / HTML / EPUB to markdown via Microsoft markitdown. Triggers: markitdown, convert document, extract docx, extract pptx, extract xlsx, .docx file, .pptx file, .xlsx file."
context: inline
effort: fast
source: official
---

# /markitdown — on-demand skill

This skill is available but not loaded into context yet.
Run `.tausik/tausik skill activate markitdown` or use `tausik_skill_activate` MCP tool to load the full version.

Requires env: MARKITDOWN_INSTALLED
Tags: documentation
