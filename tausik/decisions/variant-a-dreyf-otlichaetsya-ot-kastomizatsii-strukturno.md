---
slug: variant-a-dreyf-otlichaetsya-ot-kastomizatsii-strukturno
task: doctor-claudemd-drift-warn-never-actionable
date: "2026-07-28"
edges: []
---

## Decision

Вариант (а): дрейф отличается от кастомизации СТРУКТУРНО — расхождение тела в ОБЩЕЙ секции есть дрейф, отсутствие секции шаблона есть кастомизация. Эвристика is_trimmed_baseline удаляется. Отменяет решение doctor-accepts-trimmed-claude-md-baseline-via-heuristic-not.

## Rationale

Замер: пересечение заголовков шаблона и CLAUDE.md этого репо ПУСТО (заголовки русские), поэтому все 14 расхождений суть ABSENT-IN-CURRENT, а число одинаково для настоящего документа, пустого файла и мусорного байта — оно не замер. Новая семантика сравнивает только общие секции: переименовал секцию — структурный выбор, оставил заголовок шаблона и молча разошёлся телом — дрейф. Маркера нет, классификация выводится из структуры, подделать её в поднятом проекте нечем (AC5 конструктивно). Отвергнуто: (б) признак self-hosted «есть bootstrap/, нет .tausik-lib/» подделываем и оставляет проверку слепой здесь же; (в) только смягчить текст — сохраняет ложное число и утрату детекции, доказанную инверсией MCP-first при reported_drift=0. Прежняя эвристика (менее 6КБ + Reference на agent-contract.md) не маркер намеренности и не признак self-hosted: гасит проверку у любого с обрезанным CLAUDE.md.
