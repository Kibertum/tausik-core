---
slug: v1-7-0-full-lane
title: "Разбор находок роя v1.7.0: красный full-lane, апгрейд-путь жертвы, ложные счётчики в доке"
status: done
epic: null
story: null
complexity: null
role: developer
stack: python
tier: null
call_budget: null
defect_of: null
scope: null
scope_exclude: null
relevant_files:
  - "tests/test_mcp_integration.py"
  - "tests/test_mcp_project_server.py"
  - "bootstrap/bootstrap_opencode.py"
  - "bootstrap/bootstrap_opencode_assets.py"
  - "harness/opencode/plugins/tausik-qg0.js"
  - "scripts/service_doctor_opencode.py"
  - "tests/test_opencode_bootstrap.py"
  - "tests/test_opencode_qg0_plugin.py"
  - "tests/test_doctor_opencode.py"
  - "docs/en/mcp.md"
  - "docs/ru/mcp.md"
  - "docs/ru/architecture.md"
  - CHANGELOG.md
  - CHANGELOG.ru.md
scope_paths: []
scope_tools: []
depends_on: []
completed_at: "2026-07-14T16:45:02Z"
---

## Goal

Рой (6 ревьюеров × 3 адверсариальных опровергателя) поднял 49 находок, 10 пережили проверку. ГЛАВНОЕ: мой отчёт «4530 passed» был ЛОЖЬЮ ПО НЕВНИМАТЕЛЬНОСТИ — pyproject.toml addopts = -m 'not slow' выкидывает медленную полосу, а в ней TestMCPCrossIDEParity открывает удалённое harness/cursor/mcp и падает с FileNotFoundError. Релиз с красным полным прогоном. Плюс: апгрейд-путь ТОГО САМОГО пользователя не чинится (bootstrap мерджится в его opencode.json и сохраняет фатальный объект tools.qg0 — хост продолжит падать); плагин никогда не обновится на существующих установках (installed-копия побеждает канон); тестовый экспорт _resetCache может быть вызван загрузчиком OpenCode как плагин и уронить хост; дока противоречит сама себе в счётчиках MCP-инструментов (99/105/98/124 в одном файле).

## Acceptance Criteria

1. tests/test_mcp_integration.py::TestMCPCrossIDEParity удалён (его смысл инвертирован и покрыт test_mcp_single_canonical_tree). ОШИБКА, если full lane (pytest -m '') остаётся красным: релиз не выпускается с красной полосой, которую прячет addopts. 2. _rules_path: ОШИБКА (краш) заменена на отказ — os.path.commonpath на другом диске Windows кидает ValueError ДО проверок; плюс drive-relative 'C:evil.md' не ловится os.path.isabs. Тест: обе формы отвергаются, bootstrap не падает. 3. _resolve_plugin_source: канон из lib_dir ПОБЕЖДАЕТ installed-копию, иначе исправленный гейт никогда не доедет до существующих установок. Тест: подменить lib-версию плагина → повторный bootstrap обязан её доставить. 4. Экспорт _resetCache убран из плагина (загрузчик OpenCode может вызвать любой экспорт как plugin-factory → TypeError при инициализации = смерть хоста). Тест: в эмитированном JS ровно один экспорт. 5. АПГРЕЙД-ПУТЬ ЖЕРТВЫ: если в существующем opencode.json ключ tools НЕ объект-из-boolean (объект с вложенными объектами / список / строка) или на диске лежит singular .opencode/plugin/ — bootstrap ГРОМКО предупреждает (а doctor — fail'ит), а не рапортует успех. ОШИБКА, если bootstrap печатает успех, оставив конфиг, с которым хост не стартует. 6. doctor: tools как список или строка — тоже fail (сейчас проверяется только isinstance dict, и doctor заявляет «no tools object», когда там список). 7. Дока: убраны cursor/mcp из деревьев harness в docs/{en,ru}/adding-new-ide.md; исправлены счётчики (RU architecture 116+7=123 → 117+7=124; mcp.md EN 99/105 и RU 98 → 117/124); '33 общих скилла' → фактическое число. ОШИБКА, если в доке остаётся хоть одно упоминание удалённого harness/cursor/mcp. 8. Тесты на generate_opencode_commands и scaffold_opencode (сейчас НОЛЬ) + на то, что для ide=opencode AGENTS.md НЕ создаётся. 9. Полный прогон ОБЕИХ полос зелёный: pytest -m '' и pytest. doctor чист.

## Plan

## Rollback

## Journal

- 2026-07-14T15:39:54Z [implementation] — РОЙ (6 ревьюеров × 3 адверсариальных опровергателя, 154 агента): 49 находок поднято, 10 пережили проверку. Разобрано: [CRITICAL] Полная полоса pytest была КРАСНОЙ, а я доложил зелёный сьют. pyproject.toml addopts = -m 'not slow' деселектит медленные модули; TestMCPCrossIDEParity (tests/test_mcp_integration.py) открывал удалённое harness/cursor/mcp без проверки существования → FileNotFoundError. Воспроизвёл сам: `pytest ...TestMCPCrossIDEParity -m ""` → 1 failed; без -m "" → 1 deselected. Класс удалён (его смысл инвертирован и покрыт test_mcp_single_canonical_tree). Знание: gotcha #205. [HIGH] _rules_path КРАШИЛ bootstrap вместо отказа: os.path.commonpath кидает ValueError на чужом диске Windows, и это происходило ДО синтаксических проверок. Плюс drive-relative 'C:evil.md' не ловится os.path.isabs (ни на POSIX, ни на Windows). Порядок инвертирован: сначала дешёвые синтаксические отказы (ntpath.isabs + regex на букву диска + '..' + '.'/'/'), потом containment в try/except ValueError. 7 векторов в параметризации. [HIGH] _resolve_plugin_source предпочитал УСТАНОВЛЕННУЮ копию канону из lib → после первого bootstrap src == dst, копирование пропускается, и исправленный гейт НИКОГДА не доезжает до существующих установок. Пользователь обновил бы TAUSIK ради фикса, увидел успех и продолжил работать со сломанным гейтом. Приоритет инвертирован; тест: подменил lib-версию → повторный bootstrap обязан её доставить. [HIGH] Экспорт _resetCache из плагина. Дока OpenCode: «модуль экспортирует ОДНУ ИЛИ НЕСКОЛЬКО plugin-функций» — загрузчик зовёт экспорты как фабрики. Тестовый хелпер был бы вызван с контекстом, его undefined прочитан на хуки → TypeError при инициализации = смерть хоста из-за символа, существовавшего только для pytest. Убран; тест test_exactly_one_export стережёт. [HIGH] АПГРЕЙД-ПУТЬ ЖЕРТВЫ (находка критика полноты, никто из шести не смотрел): bootstrap мерджился в существующий opencode.json, СОХРАНЯЯ фатальный объект tools.qg0, и печатал успех — хост продолжал не стартовать. Добавлен warn_unbootable_config(): громко называет фатальное (tools не объект-из-boolean; singular .opencode/plugin/), но НЕ удаляет пользовательский файл. doctor падает на тех же условиях. [MEDIUM] doctor: tools как список/строка проходил мимо (проверялся только isinstance dict) — и doctor заявлял «valid — no tools object», подтверждая ровно то, что сломано. Гейт по ПРИСУТСТВИЮ ключа, а не по типу. [MEDIUM] Ноль тестов на generate_opencode_commands и scaffold_opencode (путь, который зовёт реальный пользователь). Добавлены + тест на то, что для opencode AGENTS.md НЕ создаётся. [HIGH×3 docs] Дока опять врала: деревья harness в adding-new-ide.md (EN+RU) показывали удалённый cursor/mcp; RU architecture 116+7=123 против реальных 117+7=124; mcp.md противоречил сам себе — 99 (список серверов), 105 (сноска RAG), 124 (заголовок), RU 98. Всё выправлено на 117/124. ЧЕСТНОСТЬ: OPENCODE_DIR/OPENCODE_BIN_PATH я не проверял на живой сборке — помечено как unverified в коде и в доке (детект по каталогу .opencode/ надёжен и не зависит от них). КРИТИК БЫЛ НЕПРАВ в одном: утверждал, что каталог команд у OpenCode singular (command/). Проверил первоисточник — plural (commands/), как у меня. Не стал править по совету, не проверив.
- 2026-07-14T15:52:11Z [implementation] — ПОЛНАЯ ПОЛОСА ДАЛА 4 ПАДЕНИЯ — БОЛЬШЕ, ЧЕМ НАШЁЛ РОЙ (он видел одно). Разделил по происхождению, проверив на ЧИСТОМ HEAD через git worktree (d:/tmp/tausik-head-check, HEAD=cdde825 — релизный коммит v1.6.1): МОИ (радиус удаления зеркала, рой нашёл один из трёх): 1. tests/test_mcp_integration.py::TestMCPCrossIDEParity — открывал harness/cursor/mcp без проверки. Удалён. 2. tests/test_mcp_project_server.py::test_cursor_project_server_chdir_is_in_main — то же. Удалён. 3. tests/test_mcp_project_server.py::test_cursor_project_server_logs_traceback_on_exception — то же. Удалён. ПЛЮС test_cursor_project_server_rejects_missing_dir: он НЕ падал — у него был @skipif(not exists(CURSOR_SERVER)), то есть после удаления зеркала он бы ВЕЧНО ПРОПУСКАЛСЯ. Мёртвый гейт, притворяющийся живым, — хуже упавшего. Удалён. НЕ МОИ — красные в main ЕЩЁ ДО ЭТОЙ СЕССИИ (проверено прогоном на HEAD, 2 failed): 4. test_project_server_minimal_text_reply_on_exception — пинил ТОЧНУЮ строку исходника 'TextContent(type="text", text=f"Error: {e}")'. Сервер давно строит reply через переменную (+usage hint), поэтому тест покраснел при том изменении и с тех пор лежал незамеченным. Переписан на проверку НАМЕРЕНИЯ: reply строится из сообщения исключения, а traceback уходит только в stderr и никогда не попадает в TextContent. 5. TestMCPHandlerDispatch::test_every_tool_name_has_handler — KeyError 'slug'. Причина: тест вёл РУЧНОЙ skip-список инструментов, требующих аргументов, и он сгнил — tausik_reason_step и tausik_task_replay добавили позже и в список не внесли. Чинить дописыванием = консервировать гниль. Вылечена причина: недостающий аргумент теперь трактуется как ДОКАЗАТЕЛЬСТВО существования хендлера (KeyError/TypeError → continue), а падением считается только «Unknown tool» — то, что тест и должен проверять. Ручной список больше не нужен, каждый будущий инструмент покрыт автоматически. ВЫВОД, который важнее самих фиксов: медленная полоса в main была КРАСНОЙ уже на релизном коммите v1.6.1. Гейт «нет коммита без зелёных тестов» на ней не работал, потому что addopts прячет её от агента. Это класс, а не случай.
- 2026-07-14T16:17:33Z [implementation] — AC verified. ПОЛНАЯ ПОЛОСА (pytest -m ""): 4667 passed, 21 skipped, 0 failed — впервые по-настоящему зелёная, включая тесты, красные ещё до этой сессии. Doctor: All clean. Gates: verify passed=True (scope critical). 1. ✓ TestMCPCrossIDEParity удалён (смысл инвертирован и покрыт test_mcp_single_canonical_tree). Full lane больше не красный. ПОДТВЕРЖДЕНО ПРОГОНОМ, а не рассуждением: до фикса `pytest ...TestMCPCrossIDEParity -m ""` → 1 failed, без -m "" → 1 deselected. 2. ✓ _rules_path: краш заменён на отказ. Порядок инвертирован — сначала дешёвые синтаксические проверки (ntpath.isabs + regex на букву диска + '..' + '.'/'/'), потом containment в try/except ValueError. os.path.commonpath на чужом диске Windows кидал ValueError ДО проверок, то есть враждебный ввод РОНЯЛ bootstrap вместо того, чтобы быть отвергнутым. Параметризация из 7 векторов, включая drive-relative 'C:evil.md' (его os.path.isabs не считает абсолютным ни на одной ОС) и UNC. 3. ✓ _resolve_plugin_source: канон из lib_dir побеждает installed-копию. Тест test_library_copy_beats_the_installed_one: ставим v1, меняем lib на v2, повторный bootstrap обязан доставить v2. До фикса src == dst → копирование пропускалось → исправленный гейт НИКОГДА не доезжал до существующих установок (пользователь обновляет TAUSIK ради фикса, видит успех, продолжает работать со сломанным гейтом). 4. ✓ Экспорт _resetCache убран; test_exactly_one_export стережёт единственность. Первоисточник (opencode.ai/docs/plugins): «модуль экспортирует одну или несколько plugin-функций» — загрузчик зовёт экспорты как фабрики, значит тестовый хелпер был бы вызван с контекстом, его undefined прочитан на хуки → TypeError при инициализации = смерть хоста из-за символа, существовавшего только для pytest. 5. ✓ АПГРЕЙД-ПУТЬ ЖЕРТВЫ: warn_unbootable_config() громко называет фатальное в существующем opencode.json (tools не объект-из-boolean; singular .opencode/plugin/), но НЕ удаляет пользовательский файл. Тесты: TestVictimUpgradePath (объект tools, список, строка, число; singular plugin/; и парный — boolean-tools молчит, чтобы предупреждение не обесценилось). До фикса bootstrap мерджился рядом с убийственным конфигом и печатал успех. 6. ✓ doctor: гейт по ПРИСУТСТВИЮ ключа tools, а не по типу. Тест: tools=["qg0"] / "qg0" / 42 → fail с упоминанием ConfigInvalidError, и НИ ОДНО сообщение не содержит «no tools object». 7. ✓ Дока: убраны cursor/mcp из деревьев harness (EN+RU adding-new-ide); RU architecture 116+7=123 → 117+7=124; mcp.md EN 99→117 и 105→124, RU 98→124; RU '33 общих скилла' → '13 core + 20 vendor'. Остаточные упоминания harness/cursor — только исторические пояснения «было и удалено», проверено grep. 8. ✓ Тесты на generate_opencode_commands (11 стабов, каталог commands/ мн.ч., чужой стаб не перезаписывается) и scaffold_opencode (конфиг+правила+плагин+команды, и AGENTS.md НЕ создаётся). Было ноль. 9. ✓ ОБЕ полосы зелёные; doctor чист. СВЕРХ AC — НАЙДЕНО ПОЛНЫМ ПРОГОНОМ, НЕ РОЕМ (рой видел 1 из 4): (а) ещё два cursor-теста в tests/test_mcp_project_server.py открывали удалённое зеркало — удалены; (б) test_cursor_project_server_rejects_missing_dir НЕ падал, у него был @skipif(not exists(CURSOR_SERVER)) — после удаления зеркала он бы ВЕЧНО ПРОПУСКАЛСЯ: мёртвый гейт, притворяющийся живым, хуже упавшего. Удалён. НАСЛЕДСТВЕННАЯ КРАСНОТА (проверено на чистом HEAD=cdde825 через git worktree — 2 failed ДО моих правок): (в) test_project_server_minimal_text_reply_on_exception пинил точную строку исходника, которая давно изменилась → переписан на проверку НАМЕРЕНИЯ (reply строится из сообщения исключения; traceback только в stderr, никогда в TextContent); (г) test_every_tool_name_has_handler вёл РУЧНОЙ skip-список, и он сгнил (reason_step, task_replay добавили позже) → вылечена причина: отсутствие аргумента трактуется как доказательство существования хендлера, падением считается только «Unknown tool»; список больше не нужен, будущие инструменты покрыты автоматически. Domain: вне тестов это значит, что медленная полоса в main была КРАСНОЙ уже на релизном коммите v1.6.1 — гейт «нет коммита без зелёных тестов» на ней не работал, потому что addopts прячет её от агента. Записано как gotcha #205: перед релизом и при любом структурном изменении гонять ОБЕ полосы.
- 2026-07-14T16:45:02Z [implementation] — AC verified. ОБЕ ПОЛОСЫ ЗЕЛЁНЫЕ (финальный прогон после разделения модуля): FULL (pytest -m "") — 4667 passed, 21 skipped, 0 failed; FAST — 4551 passed, 116 deselected, 0 failed. Doctor: All clean. Gates: verify passed=True (scope critical), filesize зелёный. 1. ✓ TestMCPCrossIDEParity удалён; full lane больше не красный. До фикса воспроизведено прогоном: `-m ""` → 1 failed, без него → 1 deselected. 2. ✓ _rules_path: краш заменён на отказ. Синтаксические проверки (ntpath.isabs + regex буквы диска + '..' + '.'/'/') идут ДО арифметики путей; containment в try/except ValueError (os.path.commonpath кидает на чужом диске Windows). 7 векторов: traversal, drive-absolute, drive-relative 'C:evil.md' (os.path.isabs не считает абсолютным НИ НА ОДНОЙ ОС), UNC, '.', '..'. 3. ✓ _resolve_plugin_source: канон из lib побеждает installed-копию. test_library_copy_beats_the_installed_one: ставим v1, меняем lib на v2, повторный bootstrap доставляет v2. До фикса src == dst → копирование пропускалось → исправленный гейт не доезжал до существующих установок никогда. 4. ✓ Экспорт _resetCache убран; test_exactly_one_export стережёт. Первоисточник: загрузчик OpenCode зовёт экспорты как plugin-фабрики → тестовый хелпер уронил бы хост TypeError при инициализации. 5. ✓ warn_unbootable_config(): фатальное в существующем opencode.json (tools не объект-из-boolean; singular .opencode/plugin/) называется ГРОМКО, но пользовательский файл не удаляется. TestVictimUpgradePath: объект/список/строка/число + singular plugin/ + парный тест (boolean-tools молчит). 6. ✓ doctor: гейт по ПРИСУТСТВИЮ ключа tools. tools=["qg0"]/"qg0"/42 → fail с ConfigInvalidError; ни одно сообщение не содержит «no tools object». 7. ✓ Дока: cursor/mcp убран из деревьев harness (EN+RU); RU architecture 123 → 124; mcp.md EN 99→117, 105→124, RU 98→124; RU '33 общих скилла' → '13 core + 20 vendor'. Остаточные упоминания harness/cursor — только исторические пояснения. 8. ✓ Тесты на generate_opencode_commands (11 стабов, commands/ мн.ч., чужой стаб не перезаписывается) и scaffold_opencode (конфиг+правила+плагин+команды; AGENTS.md НЕ создаётся). Было ноль. 9. ✓ Обе полосы зелёные; doctor чист. СВЕРХ AC — filesize gate поймал 449 строк: bootstrap_opencode.py разделён по смыслу — конфиг остался, артефакты (QG-0-плагин + командные стабы) вынесены в bootstrap_opencode_assets.py (359 + 117). Разделение сломало импорт shutil — поймано прогоном, исправлено, перепрогнано. НАЙДЕНО ПОЛНЫМ ПРОГОНОМ, НЕ РОЕМ (рой видел 1 из 4): ещё два cursor-теста в test_mcp_project_server.py; плюс test_cursor_project_server_rejects_missing_dir, который НЕ падал — у него был @skipif(not exists(CURSOR_SERVER)), то есть после удаления зеркала он ВЕЧНО ПРОПУСКАЛСЯ. Мёртвый гейт, притворяющийся живым, хуже упавшего. Все удалены. НАСЛЕДСТВЕННАЯ КРАСНОТА (проверено на чистом HEAD=cdde825 через git worktree — 2 failed ДО моих правок, т.е. медленная полоса была красной уже на релизном коммите v1.6.1): test_project_server_minimal_text_reply_on_exception пинил точную строку исходника → переписан на проверку НАМЕРЕНИЯ (reply из сообщения исключения; traceback только в stderr, никогда в TextContent). test_every_tool_name_has_handler вёл РУЧНОЙ skip-список, и он сгнил (reason_step, task_replay добавили позже) → вылечена ПРИЧИНА: отсутствие аргумента = доказательство существования хендлера, падение только на «Unknown tool»; список больше не нужен, будущие инструменты покрыты автоматически. Domain: вне тестов — гейт «нет коммита без зелёных тестов» на медленной полосе НЕ РАБОТАЛ, потому что addopts прячет её от агента. Теперь обе полосы зелёные впервые за несколько релизов. Записано как gotcha #205: перед релизом и при любом структурном изменении гонять ОБЕ.
