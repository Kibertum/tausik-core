---
slug: s154-claims-and-tests-outrun-what-the-code-does
title: "Шесть мелких расхождений в собственной партии #154: три обещания сильнее кода и три теста слабее, чем читаются"
status: done
epic: null
story: null
complexity: simple
role: developer
stack: python
tier: moderate
call_budget: 40
defect_of: projection-property-test-cannot-reach-cascades
scope: "scripts/state_triggers.py, scripts/service_decide.py, scripts/project_backend.py, tests/test_state_projection_tracks_db.py, tests/test_decide_classifies_what_it_publishes.py, CHANGELOG.md, CHANGELOG.ru.md"
scope_exclude: null
relevant_files:
  - "scripts/state_triggers.py"
  - "scripts/project_backend.py"
  - "scripts/service_decide.py"
  - "tests/test_state_projection_tracks_db.py"
  - "tests/test_decide_classifies_what_it_publishes.py"
  - CHANGELOG.md
  - CHANGELOG.ru.md
scope_paths: []
scope_tools: []
depends_on: []
completed_at: "2026-08-01T19:45:43Z"
---

## Goal

Найдено ревью сессии #154 в коде, написанном В ЭТОЙ ЖЕ СЕССИИ. Каждый пункт мелкий; собраны вместе, потому что тема одна — утверждение шире того, что обеспечено. Каждый проверен ревьюером прогоном, ссылки на замеры ниже.

ОБЕЩАНИЯ СИЛЬНЕЕ КОДА.
(1) scripts/state_triggers.py::_reproject_orphaned_edge_sources — докстринг говорит «self-healing: ребро, осиротевшее любым способом, подберётся на следующем уходе». Оно НЕ САМОИСЦЕЛЯЮЩЕЕСЯ: перепроекция ИСТОЧНИКА не убирает строку из memory_edges, поэтому предикат, по которому идёт обход, не очищается никогда. Замер ревью: связь A->B, архивация B, затем три посторонних удаления -> «sweep #1 re-exported 1 source(s); sweep #2: 0; sweep #3: 0», при этом орфан ПО-ПРЕЖНЕМУ в выборке после каждого обхода. Сеансы 2 и 3 сделали полный скан memory_edges и полный рендер источника впустую. Хуже: рёбра в АРХИВИРОВАННУЮ память осиротевают навсегда, а архив только растёт — стоимость на удаление растёт монотонно на дереве в 2100 файлов.
(2) scripts/service_decide.py::_same_file и обе записи CHANGELOG — «два действительно разных файла не могут иметь общий realpath». На Windows с включённой пофайловой чувствительностью регистра NTFS (fsutil setCaseSensitiveInfo, штатная возможность для WSL2) два файла, различающиеся регистром, существуют РАЗДЕЛЬНО; realpath вернёт два разных имени, а normcase после него сольёт их. Fail-closed гард в такой конфигурации становится fail-open ровно для того класса, ради которого написан.
(3) scripts/project_backend.py::_project_write и ::_flush_pending_projection — докстринги говорят «Never raises», но `from state_triggers import auto_export_write` в обоих стоит ВНЕ try. Сам auto_export_write защищён полностью; незащищён импорт. _project_write зовётся из _update, _flush_pending_projection — из commit_tx, поэтому ImportError сломал бы саму запись в БД, а не только проекцию.

ТЕСТЫ СЛАБЕЕ, ЧЕМ ЧИТАЮТСЯ.
(4) tests/test_decide_classifies_what_it_publishes.py::_spelling_variant на POSIX возвращает путь БЕЗ ИЗМЕНЕНИЙ, поэтому два из трёх тестов класса TestBindingGuardComparesFilesNotStrings спрашивают «тот же ли файл X, что и X» на ubuntu и macos. Регрессия по регистру защищена только на windows-latest из трёх ног матрицы CI.
(5) tests/test_state_projection_tracks_db.py::_op_task_close вызывает svc._project_task(t) вручную — в настоящем task_done такого вызова НЕТ, и ревью доказало прогоном, что дерево байт в байт одинаково без него. Строка мёртвая и стоит внутри последовательности, чья заявленная суть — «никакого ручного экспорта».
(6) tests/test_state_projection_tracks_db.py::_SqlTap — _WRITE_RE требует глагол DML первым токеном, а _IS_SELECT ловит всё, что начинается с WITH. SQLite поддерживает WITH ... UPDATE/DELETE. Замер ревью: такой оператор классифицируется как ЧТЕНИЕ, при этом реально меняет строку. Сегодня в кодовой базе CTE-DML нет, но храповик путей записи — тест, чья работа ловить запись мимо хука, — на нём останется зелёным.
Там же (7): причина исключения ("memory_edges","DELETE") сформулирована как «рёбра только мягко инвалидируются, никогда не удаляются» — фактически неверно, scripts/backend_migrations.py:61-64 делает четыре DELETE FROM memory_edges. Буквальная область исключения (сервисный слой) держится, формулировка — нет.

## Acceptance Criteria

1. (1) СХОДИМОСТЬ ОБХОДА: либо обход ДЕЛАЕТ утверждение истинным — при перепроекции источника ребро мягко инвалидируется (valid_to), и предикат очищается, — либо докстринг перестаёт называть обход самоисцеляющимся и говорит, что он повторяется на каждом уходе. Выбор обоснован в журнале. Если выбрана инвалидация: сказано, не теряется ли при этом информация, которую граф обязан помнить.
2. (1) СТОИМОСТЬ ИЗМЕРЕНА, а не оценена: число рендеров и время обхода на дереве, сопоставимом с проектным (порядка 2000 файлов), до и после. Замер в журнале. Без него неизвестно, был ли дефект дорогим.
3. (2) УТВЕРЖДЕНИЕ ПРО realpath ЛИБО СТАНОВИТСЯ ВЕРНЫМ (сравнение дополняется st_dev/st_ino там, где они доступны), ЛИБО ЗАМЕНЯЕТСЯ НА ЧЕСТНОЕ: остаточный риск на регистрочувствительном NTFS назван как принятый, с указанием, почему он приемлем. Правится и код, и обе записи CHANGELOG — сегодня обещание стоит в трёх местах.
4. (3) «Never raises» становится правдой: импорт в _project_write и _flush_pending_projection закрыт try/except, либо докстринг смягчён до «никогда не бросает, пока state_triggers импортируем». Первое предпочтительно — эти две функции стоят внутри _update и commit_tx, то есть на пути обычной записи в БД.
5. (4) ТЕСТ ПЕРЕСТАЁТ БЫТЬ ТАВТОЛОГИЕЙ НА POSIX: _spelling_variant даёт настоящее «то же имя, другое написание» и там (например, /x/../x против /x, либо переиспользуется машинерия симлинка из соседнего теста). Проверяется прогоном на POSIX-раннере с временно убранным normcase: тест обязан краснеть.
6. (5) Ручной svc._project_task в _op_task_close удалён, свойство остаётся зелёным на всех сидах — либо строка сохранена с объяснением в докстринге, почему она там, при том что заявлено «никакого ручного экспорта».
7. (6) и (7) _SqlTap перестаёт принимать WITH ... UPDATE/DELETE за чтение (снятие ведущего WITH-префикса перед классификацией) ЛИБО ограничение названо комментарием рядом с _WRITE_RE; причина исключения ("memory_edges","DELETE") переписана на фактически верную — миграции такие DELETE делают, исключение держится областью наблюдения, а не отсутствием операции.
8. Полный pytest зелёный; ruff и mypy чистые.
Записи в CHANGELOG добавляются ТОЛЬКО по пунктам (1)-(3): они меняют поведение или исправляют ложное утверждение в уже опубликованной записи. Пункты (4)-(7) — тесты и комментарии, закрывать с --no-changelog не требуется, если хотя бы один пункт даёт запись.

## Plan

## Rollback

git revert

## Journal

- 2026-08-01T19:45:40Z [implementation] — AC verified: 1. ✓ (1) СХОДИМОСТЬ: выбрана ВТОРАЯ ветка — докстринг перестал называть обход самоисцеляющимся и говорит, что он повторяется на каждом уходе. ОБОСНОВАНИЕ В ЖУРНАЛЕ И В САМОМ ДОКСТРИНГЕ: сходимость означает гашение ребра (valid_to), а делать это надо ТАМ, ГДЕ ПРОИСХОДИТ УХОД — на сервисном слое, в memory_archive и удалениях. Не в _reproject_orphaned_edge_sources: это fail-open триггер проекции, стоящий НИЖЕ базы по потоку; запись в БД оттуда инвертирует зависимость и открывает путь к повторному входу в хук. Вынесено задачей orphaned-edges-never-converge-so-every-departure-pays-for-them с семью AC, включая явное требование решить вопрос обратимости (разархивация памяти vs необратимый valid_to) — то есть вопрос «не теряется ли информация, которую граф обязан помнить» не проигнорирован, а поставлен там, где на него можно ответить. 2. ✓ (1) СТОИМОСТЬ ИЗМЕРЕНА, НЕ ОЦЕНЕНА. Мой прогон: 2000 записей памяти, 40 осиротевших рёбер, три обхода подряд — обход #1: returned=40 export_one=40 47ms орфанов осталось 40 обход #2: returned=0 export_one=40 281ms орфанов осталось 40 обход #3: returned=0 export_one=40 16ms орфанов осталось 40 «returned=0 при export_one=40» — весь дефект одной строкой: сорок сериализаций, не изменивших ни одного файла. Счёт ограничен числом ОРФАНОВ, а не размером дерева, но архивная память только растёт, поэтому плата за уход растёт монотонно. Числа перенесены В ДОКСТРИНГ, а не только в журнал. 3. ✓ (2) УТВЕРЖДЕНИЕ ПРО realpath СТАЛО ВЕРНЫМ, а не смягчено. _same_file теперь спрашивает ФАЙЛОВУЮ СИСТЕМУ: когда оба пути существуют, отвечает (st_dev, st_ino) — это и ЕСТЬ значение слов «тот же файл», и у него нет того режима отказа. Строковый путь остался ЗАПАСНЫМ (несуществующий файл, том без пригодного инода — некоторые сетевые шары Windows возвращают ноль), и остаточный риск там НАЗВАН, а не отрицён. Обе записи CHANGELOG обновлены: новая запись прямо говорит, что прежнее утверждение «два действительно разных файла не могут иметь общий realpath» ложно на NTFS с пофайловой чувствительностью к регистру (fsutil setCaseSensitiveInfo, штатный путь для WSL2), и что fail-closed гард становился там fail-OPEN ровно для своего класса. 4. ✓ (3) «Never raises» СТАЛО ПРАВДОЙ, выбран предпочтительный вариант. Импорт в _project_write и в _flush_pending_projection закрыт try/except с логированием. Обоснование в докстрингах: эти две стоят на пути из _update и из commit_tx, поэтому незащищённый ImportError сломал бы САМУ ЗАПИСЬ В БД — best-effort хук уронил бы то, по отношению к чему он best-effort. 5. ✓ (4) ТАВТОЛОГИЯ НА POSIX СНЯТА. _spelling_variant больше НИКОГДА не возвращает вход без изменений: на Windows — флип регистра буквы диска (испытывает normcase), на POSIX — <dir>/./<base>/../<base> (испытывает realpath). ОТСТУПЛЕНИЕ ОТ БУКВЫ АС, названное прямо: АС требует «прогон на POSIX с временно убранным normcase обязан краснеть», но os.path.normcase на POSIX ЕСТЬ ТОЖДЕСТВО — никакая проба не может сделать его снятие красным, и утверждать обратное значило бы повторить ровно ту ошибку, которую чинит эта задача. Что МОЖНО испытать на POSIX — вторую половину гарда: без realpath строки не равны, тест краснеет. Плюс добавлена МЕХАНИЧЕСКАЯ проба test_the_variant_is_never_the_input_unchanged: вариант обязан отличаться КАК СТРОКА и при этом называть тот же файл — она краснеет на ЛЮБОЙ платформе, если помощник снова начнёт возвращать вход. Раньше это ловилось только чтением кода. 6. ✓ (5) Ручной svc._project_task(t) из _op_task_close УДАЛЁН, свойство зелёное на всех шести сидах (20 passed в файле). На его месте — комментарий, объясняющий, почему строки там быть не должно: настоящий task_done такого вызова не делает, дерево без него байт в байт то же, а ручной экспорт внутри цикла свойства есть ровно то, что свойство призвано доказать ненужным. 7. ✓ (6) и (7). _SqlTap перестал принимать WITH ... UPDATE/DELETE за чтение: добавлен _strip_cte_prefix, снимающий ведущий CTE-префикс СО СЧЁТОМ СКОБОК, а не регуляркой — подзапрос может содержать и запятую, и закрывающую скобку, на которых шаблон остановился бы. Закреплено параметризованным тестом test_the_tap_sees_a_write_hiding_behind_a_cte из пяти случаев, включая WITH RECURSIVE, вложенный подзапрос с запятой и скобкой, и НЕГАТИВ (CTE перед SELECT по-прежнему НЕ считается записью). Причина исключения ("memory_edges","DELETE") переписана на фактически верную: не «рёбра никогда не удаляются» (backend_migrations.py делает четыре DELETE FROM memory_edges), а «ни один СЕРВИСНЫЙ метод их не удаляет» — исключение держится ОБЛАСТЬЮ НАБЛЮДЕНИЯ, а не отсутствием оператора. 8. ✓ Полный pytest: 6556 passed, 24 skipped, 0 failed, 0 errors (661s). ruff check — All checks passed. mypy — Success: no issues found in 294 source files. bootstrap --ide all прогнан перед прогоном, drift отсутствует (гейт зелёный). 9. ✓ CHANGELOG по пунктам (1)-(3), как требует условие — записи добавлены в CHANGELOG.md [Unreleased] и в зеркало CHANGELOG.ru.md, тремя абзацами: импорт под «Never raises», несходимость обхода с числами 40/40/40, и переход гарда на (st_dev, st_ino) с названным остаточным риском. Пункты (4)-(7) — тесты и комментарии, отдельных записей не получили. Domain: результат осмыслен вне тестов. Два из трёх пунктов — реальное изменение поведения на живой системе: ImportError в state_triggers больше не способен уронить запись в БД (проверяется структурно — импорт внутри try), а сравнение баз данных теперь опирается на инод, то есть на то, чем файловая система ОТВЕЧАЕТ на вопрос «тот же ли это файл», а не на нормализацию строки, которая на регистрочувствительном томе даёт неверный ответ. Третий пункт — измерение, и его число (40 бесполезных сериализаций на каждый уход, растущее с архивом) — наблюдаемая характеристика системы, а не свойство теста. Root cause (documentation): все три «обещания сильнее кода» написаны по НАМЕРЕНИЮ автора, а не по тому, что делает написанный им код, и написаны в ТОЙ ЖЕ сессии, что и код — то есть проверять было нечего, утверждение и реализация родились вместе и разошлись сразу. Три «теста слабее, чем читаются» — тот же корень с другой стороны: тест назван по свойству, а проверяет частный случай, и на части платформ вырождается в тавтологию. Prevention: утверждение о свойстве («никогда не бросает», «самоисцеляется», «два разных файла не могут») обязано иметь пробу, которая краснеет при его неверности, и проба проверяется ПОТЕСТОВО (память #360); а помощник теста, который на какой-то платформе может вернуть вход без изменений, обязан иметь механическую проверку этого — иначе вырождение видно только чтением.
- 2026-08-01T19:46:14Z [done] — ТРИ ПРЕДУПРЕЖДЕНИЯ ЗАКРЫТИЯ — все три принимаю как ВЕРНЫЕ, не отмахиваюсь. 1. «COMPLEXITY UNDERSTATED: declared simple, touched 5 behaviour-bearing of 7». ЭТО ПРАВДА, и здесь оно НЕ ложное — в отличие от случаев, описанных памятью #153, где предупреждение срабатывало на файлах ПРОЕКЦИИ. В relevant_files этой задачи проекции нет ни одной: пять поведенческих файлов — state_triggers.py, project_backend.py, service_decide.py и два тестовых. Задача была заведена как «шесть мелких расхождений», а по факту тронула три модуля продукта, включая обычный путь записи в БД. Правильная сложность — medium. Ошибка планирования, не гейта. 2. «call_actual=95 при call_budget=40» — перерасход в 2.4 раза. Причина названа честно: бюджет 40 ставился под «мелкие правки текста», а пункт (1) потребовал НАПИСАТЬ ПРОБУ на 2000 записей (AC2 требовал замер, а не оценку), пункт (2) — переписать сравнение на инодную идентичность, пункт (6) — написать разбор CTE-префикса со счётом скобок плюс параметризованный тест на пять случаев. Для калибровки: задача с формулировкой «N мелких расхождений» стоит НЕ суммы мелких правок, потому что каждое расхождение тянет за собой доказательство, а доказательство дороже правки. 3. Ссылки на тесты и негатив в evidence. Тесты, покрывающие эту задачу, ПОИМЁННО: tests/test_state_projection_tracks_db.py::test_the_tap_sees_a_write_hiding_behind_a_cte (пункты 6-7), ::test_projection_tracks_db_after_every_mutation на шести сидах (пункт 5, снятие ручного _project_task), tests/test_decide_classifies_what_it_publishes.py::TestBindingGuardComparesFilesNotStrings::test_the_variant_is_never_the_input_unchanged (пункт 4), ::test_case_differing_drive_letter_is_the_same_project_db и ::test_the_throwaway_message_is_unreachable_for_a_casing_difference (пункты 2 и 4). Negative: негативные сценарии проверены явно, а не подразумеваются. (а) test_the_tap_sees_a_write_hiding_behind_a_cte содержит случай «WITH d AS (SELECT 1) SELECT * FROM tasks» -> НЕ запись: разбор CTE не должен превращать чтение в запись, и тест краснеет, если превратит. (б) test_the_variant_is_never_the_input_unchanged краснеет, если помощник вернёт вход без изменений — то есть ловит именно вырождение в тавтологию, ради которого пункт (4) и существует. (в) Пункт (5): свойство обязано остаться зелёным на ВСЕХ шести сидах ПОСЛЕ снятия ручного вызова — если бы вызов был несущим, набор покраснел бы. (г) Пункт (2): запасной строковый путь сохранён именно для случая, когда инод недоступен, и это негатив к самому фиксу — он не должен ломать сравнение там, где stat не отвечает.
