---
slug: skill-cli-negative-tests-never-reach-the-skill-layer
title: "CI красный: два негативных теста skill CLI упираются в отказ «нет проекта» и больше не проверяют то, ради чего написаны"
status: done
epic: null
story: null
complexity: medium
role: developer
stack: python
tier: moderate
call_budget: 60
defect_of: full-suite-runs-only-in-ci-and-ci-has-not-run
scope: "tests/test_skill_cli_help.py"
scope_exclude: "scripts/project_config.py — гейт assert_project_exists корректен и остаётся как есть; scripts/skill*.py — продуктовый слой не меняется."
relevant_files:
  - "tests/test_skill_cli_help.py"
scope_paths: []
scope_tools: []
depends_on: []
completed_at: "2026-08-25T19:15:09Z"
---

## Goal

ПЕРВЫЙ ЖЕ ПОЛНЫЙ ПРОГОН В CI (run 32881343325, github/main = 5ea714f) КРАСНЫЙ: 2 failed, 7095 passed. Падают tests/test_skill_cli_help.py::TestSkillNegativeExitCode::test_install_unknown_skill и ::test_repo_add_untrusted_url. Воспроизведено локально детерминированно: pytest tests/test_skill_cli_help.py -m '' даёт 2 failed, 3 passed.

ПРИЧИНА НАЙДЕНА. Оба теста зовут CLI с cwd=tmp_path, где проекта TAUSIK нет. Гейт assert_project_exists (scripts/project_config.py) отказывает ДО того, как управление доходит до слоя skill, и печатает «Здесь нет проекта TAUSIK». Гейт приехал 2026-08-12 коммитом b487f87, тест последний раз трогали 2026-07-20. Между этими датами тест ничего не проверял, и никто этого не видел: файл помечен pytestmark = pytest.mark.slow, а лента по умолчанию идёт с -m 'not slow'.

ЦЕНА. Утеряны ровно те гарантии, ради которых тесты написаны: отказ называет неизвестный навык, и отказ подсказывает --force. Сегодня они не проверяются ничем. Третий тест того же класса, test_activate_unknown_skill, ЗЕЛЁНЫЙ ЛОЖНО — он утверждает только startswith('Error: '), чему удовлетворяет и отказ про отсутствие проекта.

ЭТО ПЕРВАЯ ДОБЫЧА СТРАХОВОЧНОЙ СЕТИ. Лента полного прогона в CI натянута в сессии #181 (память #414), и первым же запуском поймала регресс, прожившую 13 дней. Сеть работает; чинить надо улов.

КРИТЕРИИ ПРИЁМКИ: 1) tests/test_skill_cli_help.py -m '' зелёный целиком; 2) оба теста проверяют СОДЕРЖАНИЕ отказа слоя skill (имя навыка, подсказка --force), то есть доходят до него, а не подменяют проверку на более слабую; 3) test_activate_unknown_skill перестаёт быть ложнозелёным — утверждает содержание, а не только префикс; 4) НЕГАТИВНЫЙ СЦЕНАРИЙ: если отказ снова начнёт приходить с чужого слоя, тест краснеет — проверяется, что в stderr НЕТ текста про отсутствие проекта; 5) живой проект репозитория не мутируется, изоляция через tmp_path сохранена; 6) полный прогон -m '' по всему дереву зелёный.

## Acceptance Criteria

1) tests/test_skill_cli_help.py прогоняется с -m '' целиком зелёным.
2) test_install_unknown_skill и test_repo_add_untrusted_url проверяют СОДЕРЖАНИЕ отказа слоя skill: первый — что в stderr названо имя неизвестного навыка nonexistent-skill-xyz, второй — что есть подсказка --force. Проверка не ослабляется до префикса.
3) test_activate_unknown_skill перестаёт быть ложнозелёным: утверждает содержание отказа, а не только startswith('Error: ').
4) НЕГАТИВНЫЙ СЦЕНАРИЙ: все три теста краснеют, если отказ приходит с чужого слоя — явно утверждается, что в stderr НЕТ текста «Здесь нет проекта TAUSIK». Проверено вживую откатом фикса: без него тесты падают именно на этом утверждении.
5) Живой проект репозитория не мутирован: изоляция через tmp_path сохранена, git status по scripts/ и .tausik/ чист от посторонних изменений.
6) Полный прогон по всему дереву с -m '' зелёный (0 failed).

## Plan

## Rollback

git revert коммита с фиксом. Изменения ограничены tests/test_skill_cli_help.py — тестовый файл, отката схемы или миграции не требуется, продуктовый код не затрагивается.

## Journal

- 2026-08-25T18:18:47Z [implementation] — ПРИЧИНА ЛОКАЛИЗОВАНА И ПОДТВЕРЖДЕНА ДАТАМИ. Гейт assert_project_exists приехал 2026-08-12 коммитом b487f87; tests/test_skill_cli_help.py последний раз правился 2026-07-20 (590067f). Тринадцать дней три негативных теста упирались в отказ «Здесь нет проекта TAUSIK» и не доходили до слоя skill. Не видно было потому, что файл помечен pytestmark = pytest.mark.slow, а addopts в pyproject.toml = -m 'not slow': локальный прогон их ДЕСЕЛЕКТИТ (5 deselected in 0.15s), и прошлый «полный прогон 7076 passed» их не содержал. ЛОЖНАЯ ЗЕЛЕНЬ ИЗМЕРЕНА, А НЕ ПРЕДПОЛОЖЕНА. Из трёх тестов класса падали два — те, что утверждали СОДЕРЖАНИЕ (имя навыка, подсказку --force). Третий, test_activate_unknown_skill, был зелёным ЛОЖНО: он утверждал только startswith('Error: '), чему отказ про отсутствие проекта удовлетворяет. То есть слабая проверка не просто слабее — она СКРЫЛА тот же самый регресс. ФИКС: изоляция сохранена, но проект теперь настоящий. Фикстура project поднимает пустой проект в tmp_path через публичный вход `tausik init` (единственная команда, которой позволено создавать), а не раскладыванием файлов руками. Живой репозиторий не трогается. СТРАЖ ОТ ПОВТОРА. Введён _assert_refusal_came_from_the_skill_layer: отказ обязан быть про навык, и в stderr НЕ должно быть головы сообщения про отсутствие проекта. Голова берётся ИЗ ПРОДУКТА — project_config.project_missing_message('<handle>').split(':',1)[0] — а не переписывается литералом, иначе при смене формулировки страж превратится в проверку музейного текста. Импорт через sys.path.insert(0, scripts) — конвенция файла tests/test_init_creates_where_you_stand.py:32. test_activate_unknown_skill усилен: теперь утверждает имя навыка, а не префикс. ЗАМЕРЫ: tests/test_skill_cli_help.py -m '' → 5 passed in 2.52s (было 2 failed, 3 passed). AC4 доказан ДВУСТОРОННИМ опытом, а не односторонним: (1) реальный запуск CLI вне проекта — страж падает; (2) настоящий отказ слоя skill внутри проекта — страж проходит. Без второго опыта первый ничего не значил бы: страж, падающий всегда, тоже упал бы. Запущен полный прогон -m '' по всему дереву для AC6.
- 2026-08-25T19:12:59Z [implementation] — AC6 ЗАКРЫТ, НО СО ВТОРОЙ ПОПЫТКИ И С ОБХОДОМ — это часть результата, а не сноска. ПЕРВЫЙ полный прогон -m '' по всему дереву ОБОРВАЛСЯ на 86% с exit 1. Оборвал не упавший тест, а сторож зависаний: «Timeout (0:01:00)!» на tests/test_stress.py::TestStressSessions::test_100_sessions. Итоговой строки в логе нет вообще — pytest убит faulthandler_exit_on_timeout до неё. Последние 14% дерева тем прогоном не проверены. ВТОРОЙ прогон с -o faulthandler_timeout=300 (порог только на запуск, pyproject.toml НЕ тронут): 7209 passed, 24 skipped, 0 failed за 1657.95s (27:37). Дерево зелёное, AC6 выполнен. ЗАМЕР ПОД НАГРУЗКОЙ ОКАЗАЛСЯ ХУЖЕ ИЗОЛИРОВАННОГО, И ЭТО МЕНЯЕТ ВЫВОД. В одиночку test_100_sessions даёт 54.82s (запас до порога 1.09x — «почти впритык»). В полном прогоне тот же тест даёт 77.09s, то есть 128% порога. Значит сторож срабатывает НЕ маргинально, а ДЕТЕРМИНИРОВАННО: полная лента на Windows не может дойти до конца в принципе, а не «иногда не доходит». Односторонний замер в изоляции дал бы мягкий и неверный вывод — цифру надо брать в тех условиях, в которых отказ и наступает. Задача hang-guard-promises-eleven-times-headroom-and-has-one поправлена этим числом. ФИКС ЭТОЙ ЗАДАЧИ К ОБРЫВУ ОТНОШЕНИЯ НЕ ИМЕЕТ: тронут один файл tests/test_skill_cli_help.py, он проходит на 3% дерева, задолго до стресс-блока, и зелёный в обоих прогонах.
- 2026-08-25T19:15:33Z [done] — Root cause (regression): гейт assert_project_exists приехал 2026-08-12 (b487f87) и стал отказывать раньше слоя skill; тесты tests/test_skill_cli_help.py, правленные 2026-07-20 (590067f), звали CLI из tmp_path без проекта и с тех пор проверяли чужой отказ. Регрессия прожила 13 дней невидимой, потому что файл помечен pytest.mark.slow, а addopts = -m 'not slow' его деселектит: локально это выглядит как «5 deselected», а не как провал. Prevention: введён _assert_verify_refusal_came_from_the_skill_layer — общий страж, утверждающий, что отказ пришёл С НУЖНОГО СЛОЯ, а не только начинается с 'Error: '; эталон берётся из продукта (project_config.project_missing_message), поэтому смена формулировки увозит стража за собой. Плюс полная лента натянута в CI (сессия #181) — именно она и предъявила регрессию. Domain: результат осмыслен вне тестов. Проверено ВЖИВУЮ ручным запуском CLI, а не только через pytest: в каталоге без проекта `skill install nonexistent-skill-xyz` печатает «Error: Здесь нет проекта TAUSIK…», а в поднятом через `tausik init` проекте — «Error: Skill 'nonexistent-skill-xyz' not found in any repo…»; `skill repo add https://example.com/foo.git` там же даёт «Untrusted skill repository URL… retry with --force». То есть слой skill действительно отвечает по существу, и тест теперь спрашивает то, что продукт реально говорит пользователю. AC-1: ✓ tests/test_skill_cli_help.py -m '' → 5 passed in 2.52s (до фикса 2 failed, 3 passed). AC-2: ✓ test_install_unknown_skill утверждает 'nonexistent-skill-xyz' in stderr; test_repo_add_untrusted_url утверждает '--force' in stderr. Проверки не ослаблены, а усилены: к содержанию добавлен страж слоя. AC-3: ✓ test_activate_unknown_skill больше не ложнозелёный — утверждает имя навыка, а не startswith('Error: '). AC-4: ✓ доказано ДВУСТОРОННИМ опытом (scratchpad probe_guard_fires.py): вне проекта страж падает со словами «отказ пришёл не со слоя skill»; внутри проекта на настоящем отказе слоя skill страж проходит. Односторонний опыт не отличил бы стража от заглушки, падающей всегда. AC-5: ✓ живой проект не мутирован: git status показывает только tests/test_skill_cli_help.py, CHANGELOG.md, CHANGELOG.ru.md, CLAUDE.md/AGENTS.md и проекции задач. Изоляция через tmp_path сохранена, проект в тесте поднимается публичным `tausik init`. AC-6: ✓ полный прогон -m '' по всему дереву: 7209 passed, 24 skipped, 0 failed за 1657.95s. С оговоркой, вынесенной в отдельную задачу: первый прогон оборвался на 86% сторожем зависаний, зелёный получен с -o faulthandler_timeout=300 (pyproject.toml не тронут).
