---
slug: firewall-scans-heredoc-payload-not-just-the-command
title: "Командный файрвол сканирует тело heredoc, а не только исполняемую команду — написать о защите нельзя тем же каналом, который её проверяет"
status: planning
epic: arch-debt-post-18
story: adp18-quality-signals
complexity: simple
role: developer
stack: null
tier: moderate
call_budget: 30
defect_of: null
scope: null
scope_exclude: null
relevant_files: []
scope_paths:
  - "scripts/hooks/bash_firewall.py"
  - "tests/*"
scope_tools: []
depends_on: []
completed_at: null
---

## Goal

Найдено вживую в сессии #160. Попытка записать handoff, в ТЕКСТЕ которого упомянута опасная команда очистки домашнего каталога (как описание changelog-записи о защите от неё), была заблокирована файрволом как сама эта команда. Файрвол читает всю строку вызова, включая данные heredoc, и не отличает 'выполнить' от 'записать как текст'. Последствие: документацию, changelog-записи и заметки о защите невозможно провести через Bash — а именно они обязаны цитировать опасную форму, чтобы быть понятными. Ложное срабатывание, а не ложный пропуск: безопасность не ослаблена, но канал непригоден.

## Acceptance Criteria

AC1: команда, где опасная форма встречается ТОЛЬКО внутри тела heredoc или в кавычках как данные, не блокируется.
AC2: та же опасная форма как ИСПОЛНЯЕМАЯ команда по-прежнему блокируется — покрытие не ослаблено ни на один случай из нынешнего набора тестов.
AC3 (негативный): если разделить 'данные' и 'команду' надёжно нельзя, задача закрывается как dead end с записанной причиной, а файрвол остаётся строгим. Ослабление проверки ради удобства записи НЕ принимается: ложный пропуск здесь стоит дороже ложного срабатывания.

## Plan

## Rollback

Изменение в scripts/hooks/bash_firewall.py; git revert одного коммита возвращает нынешнее поведение.

## Journal

- 2026-08-03T14:55:33Z [planning] — ВТОРАЯ ДВЕРЬ, найдена в той же сессии #161. Дефект не единичный: гейт публикации (scripts/hooks/git_push_gate.py) заблокировал отправку отчёта в мессенджер за то, что в ТЕЛЕ heredoc был написан текст команды публикации. Исполнялся при этом curl. Значит форма шире, чем найденная точка (конвенция #361): не 'файрвол читает heredoc', а 'КАЖДЫЙ хук, читающий строку вызова, не отличает исполняемую команду от данных'. Оба случая — ложные срабатывания: безопасность не ослабла, но канал непригоден ровно для того текста, который обязан цитировать опасную форму, чтобы быть понятным (документация, changelog, отчёты, заметки к тегу). Область задачи стоит расширить с bash_firewall.py на общий разбор строки вызова, которым пользуются оба хука. Иначе починка одного оставит второй.
