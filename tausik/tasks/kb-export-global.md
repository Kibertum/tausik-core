---
slug: kb-export-global
title: "Бэкап общей базы знаний: единственная точка отказа не должна прожить ни одной сессии"
status: done
epic: shared-knowledge
story: kb-git-sync
complexity: medium
role: developer
stack: python
tier: null
call_budget: null
defect_of: null
scope: null
scope_exclude: null
relevant_files:
  - "scripts/knowledge_export.py"
  - "scripts/project_parser.py"
  - "scripts/project_cli_extra.py"
  - "scripts/project.py"
  - "tests/test_knowledge_export.py"
  - CHANGELOG.md
  - CHANGELOG.ru.md
scope_paths:
  - "scripts/knowledge_export.py"
  - "scripts/project_parser.py"
  - "scripts/project_cli_extra.py"
  - "scripts/project.py"
  - "tests/test_knowledge_export.py"
  - CHANGELOG.md
  - CHANGELOG.ru.md
scope_tools: []
depends_on: []
completed_at: "2026-08-02T19:09:57Z"
---

## Goal

ПЕРЕФОРМУЛИРОВАНА (сессия #117). Исходная задача обосновывалась через git как канал обмена и через «механизм уже написан» — оба основания отменены: обмен через git владельцем снят, kb-import-tasks удалена. Но ВТОРОЕ обоснование от них не зависело и осталось в силе целиком.  ПРОБЛЕМА: общая база знаний живёт на одной машине в папке пользователя без резервной копии. Это единственная точка отказа для всей накопленной за годы памяти — умрёт диск, и пропадёт всё, что фреймворк знает. Проектная БД при этом бэкапится (.db.bak.*), поэтому миграцию km-2 откатить можно; эти два риска НЕ путать — у общей базы страховки нет вообще.  РЕШЕНИЕ: бэкапу нужен любой remote, а не git. Механизм самостоятельный, на выгрузку задач не опирается. Формат — по конвенции #231 (один файл на сущность, стабильный порядок полей), чтобы бэкап был инспектируем, а не непрозрачным дампом.  МЕСТО В ПОРЯДКЕ РАБОТ: сразу после kb-global-schema, НЕ в конце фазы 3. Общая база без копии не должна прожить ни одной сессии — задача существует ровно затем, чтобы окна незастрахованности не возникло.

## Acceptance Criteria

1. Бэкап общей базы снимается одной командой и не требует git. Назначение — ТОЛЬКО ЛОКАЛЬНОЕ (каталог или том). Сетевые назначения (s3://, http://, https://, ftp://, UNC-пути вида \сервер\шара) ОТВЕРГАЮТСЯ с внятной ошибкой, называющей причину. ИЗМЕНЕНО решением #219: прежняя формулировка допускала «S3-совместимый», а на записи в общую базу вычистки нет — то есть выгрузка наружу вынесла бы невычищенное содержимое, включая абсолютные пути с именами заказчиков у КАЖДОЙ строки. Образец валидации назначения уже есть: state_serialize.assert_export_target.
2. Восстановление ПРОВЕРЕНО сквозным тестом: база убита, восстановлена из бэкапа, содержимое совпадает. ИЗМЕНЕНО решением #219: прежняя формулировка требовала сохранности memory_edges, а такой таблицы в схеме общей базы НЕТ ВООБЩЕ — критерий был непроверяем как написан. Проверять надо совпадение трёх таблиц (memory, decisions, snippets) по entry_uuid, который для этого и заведён.
3. Выгрузка ЛОГИЧЕСКАЯ, а не побайтовая копия файла. Формат по конвенции #231: один файл на сущность, детерминированный порядок полей, читается глазами. ДОБАВЛЕНО решением #219 как явное требование: побайтовая копия несовместима с этим критерием и со следующим — .db не читается глазами и не байт-стабилен из-за freelist и WAL, — а также несёт теневые таблицы FTS5 с токенизированными копиями того же содержимого и незатёртые страницы удалённых записей.
4. Выгружается замысел, НЕ доказательства (гейты, квитанции, подписи не превращаются в редактируемый текст).
5. Повторный бэкап идемпотентен: без изменений в базе не порождает нового диффа.
6. НЕГАТИВНЫЙ СЦЕНАРИЙ: отсутствие или недоступность назначения сообщается ГРОМКО (fail-closed), а не деградирует молча в «бэкап не сделан». Тест проверяет обе половины: что команда падает И что молчаливого успеха нет.
CHANGELOG.md [Unreleased] и зеркало CHANGELOG.ru.md обновлены прозаической записью.

## Plan

## Rollback

git revert; общая база остаётся только локальной

## Journal

- 2026-08-02T19:08:54Z [implementation] — ЧЕК-ЛИСТ ВЕРИФИКАЦИИ (SENAR Rule 5) — поимённые ссылки. AC-1 (одна команда, только ЛОКАЛЬНЫЕ назначения): ✓ tests/test_knowledge_export.py::TestRemoteDestinationsAreRefused::test_a_remote_destination_raises[s3://bucket/knowledge] ✓ ...[https://example.com/backup] ...[http://...] ...[ftp://...] ...[sftp://...] ...[gs://bucket/x] ✓ ...[\\server\share\backup] ...[//server/share/backup] ✓ ...::test_the_refusal_explains_why_rather_than_just_refusing ✓ ...::test_ordinary_local_paths_are_accepted[D:/backups/kn] и три других ✓ ...::test_a_windows_drive_is_not_mistaken_for_a_url_scheme ✓ ...::test_an_empty_destination_is_refused AC-2 (восстановление проверено сквозным тестом, сверка ПО СОДЕРЖИМОМУ): ✓ tests/test_knowledge_export.py::TestTheRoundTripPreservesContent::test_every_field_survives_export_and_restore ✓ ...::test_the_windows_path_comes_back_intact ✓ ...::test_embedded_newlines_and_quotes_come_back_intact ✓ ...::test_a_record_with_an_empty_string_survives ✓ ...::test_restoring_twice_converges_instead_of_doubling ✓ ...::test_counts_are_inserts_not_files_read ✓ ...::test_a_conflict_that_is_not_an_identity_conflict_raises ✓ ...::test_a_failed_restore_commits_nothing AC-3 (логическая выгрузка, один файл на запись, детерминированный порядок): ✓ tests/test_knowledge_export.py::TestTheBackupIsReadableAndStable::test_one_file_per_record_with_a_readable_name ✓ ...::test_field_order_is_fixed_not_taken_from_the_cursor ✓ ...::test_every_backup_file_is_readable_text_not_a_binary_dump ✓ ...::test_the_field_list_matches_the_real_schema (судья — PRAGMA table_info, а не константа модуля) AC-5 (идемпотентность): ✓ tests/test_knowledge_export.py::TestTheBackupIsReadableAndStable::test_a_second_backup_of_an_unchanged_store_writes_no_files ✓ ...::test_a_deleted_record_disappears_from_the_backup AC-6 (негативный сценарий, обе половины): ✓ tests/test_knowledge_export.py::TestFailuresAreLoud::test_backing_up_a_store_that_does_not_exist_raises ✓ ...::test_restoring_from_a_missing_directory_raises ✓ ...::test_restoring_from_a_directory_that_is_not_a_backup_raises ✓ ...::test_a_backup_from_a_newer_schema_is_refused СВЕРХ AC — по находкам ревью (разрушительная сверка): ✓ tests/test_knowledge_export.py::TestTheBackupNeverDeletesSomeoneElsesFiles::test_a_directory_with_foreign_files_is_refused ✓ ...::test_an_empty_directory_is_fine ✓ ...::test_a_previous_backup_directory_is_reused ✓ ...::test_pruning_spares_a_file_that_is_not_a_record ✓ ...::test_a_record_whose_identity_is_not_a_safe_filename_is_refused ПРОБЫ ФАЛЬСИФИЦИРУЕМОСТИ — РУЧНЫЕ ПРОГОНЫ, обратимыми правками scripts/knowledge_export.py: ✓ наивная цепочка replace вместо посимвольного разворота -> 2 failed, включая круговорот и путь Windows ✓ сверка перестала щадить непринадлежащие файлы -> красный test_pruning_spares_a_file_that_is_not_a_record ✓ снят гард каталога назначения -> красный test_a_directory_with_foreign_files_is_refused ✓ возврат к INSERT OR IGNORE -> красный test_a_conflict_that_is_not_an_identity_conflict_raises ✓ после каждого восстановления 38 passed ПРОВЕРКА ЗАПУСКОМ АРТЕФАКТА (память #346), настоящий CLI после bootstrap: ✓ `tausik knowledge export --to s3://bucket/kn` -> отказ с причиной ✓ `tausik knowledge export --to <локальный>` -> «Backed up 2 record(s) ... (1 memory, 1 decisions, 0 snippets)» ✓ файлы на диске: manifest.md, memory/<uuid>.md, decisions/<uuid>.md; origin_project экранирован как "D:\\Work\\..." — то самое место, где наивный разбор ломался Полный набор: «6680 passed, 24 skipped, 140 deselected in 983.40s», 0 failed, 0 errors. ruff check All checks passed, ruff format already formatted, mypy Success: no issues found in 298 source files. bootstrap --ide all прогнан.
- 2026-08-02T19:09:54Z [implementation] — AC verified (поимённые ссылки на 30 тестов — в журнале задачи): 1. ✓ ОДНА КОМАНДА, ТОЛЬКО ЛОКАЛЬНЫЕ НАЗНАЧЕНИЯ. `tausik knowledge export --to <кат>` и `restore --from <кат>`. Восемь форм сетевых назначений отвергаются (s3, gs, http, https, ftp, sftp, две формы UNC), и отказ НАЗЫВАЕТ ПРИЧИНУ, а не просто отказывает. Проверка по ФОРМЕ, а не по достижимости: недоступный сегодня remote всё равно remote. Отдельно: буква диска Windows не принимается за схему URL (urlparse читает «D:/x» как схему «d» — различает длина). Осознанно терпимо к смонтированным томам, и это записано, а не умолчано: на этом уровне смонтированная шара неотличима от локального диска, а отказ всякому монтированию отказал бы внешнему диску — самой вероятной цели бэкапа. 2. ✓ ВОССТАНОВЛЕНИЕ ПРОВЕРЕНО СКВОЗНЫМ ТЕСТОМ, И СВЕРКА ИДЁТ ПО СОДЕРЖИМОМУ, поле за полем, а не по числу строк. Вход намеренно скверный: путь Windows с обратными слэшами, встроенный перенос строки, кавычки, табуляция, кириллица, пустая строка. Плюс атомарность: отказ на середине не коммитит НИЧЕГО. 3. ✓ ЛОГИЧЕСКАЯ ВЫГРУЗКА, один файл на запись, детерминированный порядок полей. Порядок задан КОНСТАНТОЙ, а не курсором: иначе добавление колонки перетасовало бы каждый файл. Проверка «файл не бинарный» смотрит СОДЕРЖИМОЕ, а не расширение. И отдельный тест сверяет список полей с РЕАЛЬНОЙ схемой через PRAGMA table_info — иначе тест сверял бы модуль сам с собой и забытая колонка осталась бы невидимой. 4. ✓ ВЫГРУЖАЕТСЯ ЗАМЫСЕЛ, НЕ ДОКАЗАТЕЛЬСТВА. В FIELDS только содержательные колонки трёх таблиц; ни гейтов, ни квитанций, ни подписей в общей базе нет вовсе. 5. ✓ ИДЕМПОТЕНТНОСТЬ. Повторный бэкап неизменной базы не открывает на запись НИ ОДНОГО файла (тест считает вызовы open, а не сравнивает время: разрешение таймера на Windows ~15 мс, и сравнение времени прошло бы ЛОЖНО). Удалённая запись исчезает из бэкапа — иначе восстановление воскресило бы её. 6. ✓ НЕГАТИВНЫЙ СЦЕНАРИЙ, ОБЕ ПОЛОВИНЫ. Нечего бэкапить, нет каталога, каталог не похож на бэкап, манифест с нечисловой версией, бэкап более новой схемы — всё поднимает ServiceError и говорит, что НЕ было сделано. Отдельно проверено, что отказ не оставляет за собой созданного каталога. CHANGELOG.md и зеркало CHANGELOG.ru.md — прозаическая запись. ЧТО ПОЙМАЛ САМ, ДО ПРОГОНА: писатель frontmatter экранирует обратный слэш ПЕРВЫМ, поэтому разворот цепочкой замен превратил бы литеральный слэш с буквой n в перенос строки. Путь проекта есть у КАЖДОЙ записи — значит каждая строка восстанавливалась бы испорченной С ОТЧЁТОМ ОБ УСПЕХЕ. Прочитал код писателя вместо предположения; ревью независимо подтвердило, что разворот теперь ТОЧНАЯ инверсия (проверено на 39 состязательных значениях, 0 расхождений). МУЛЬТИАГЕНТНОЕ РЕВЬЮ ДВУМЯ ЛИНЗАМИ — ВСЕ НАХОДКИ ЗАКРЫТЫ: КРИТИЧЕСКОЕ, РАЗРУШИТЕЛЬНОЕ, ВОСПРОИЗВЕДЕНО РЕВЬЮЕРОМ ЖИВЬЁМ: сверка удаляла ЧУЖИЕ файлы. Ревьюер положил в каталог написанный человеком ADR и получил его исчезновение после одного экспорта. Хуже: state_export пишет decisions/ и memory/ в дерево проекта — ТЕ ЖЕ имена, — то есть --to на это дерево удалил бы записи самого проекта. Мой докстринг при этом утверждал, что сверка «не может дотянуться до того, чего экспорт не создавал». Починено ДВУМЯ рубежами: каталог обязан быть пустым либо уже быть нашим бэкапом (отказ до единого записанного байта), и сверка щадит любой файл, не названный по записи. Плюс идентичность валидируется как имя файла, а не склеивается в путь. КРИТИЧЕСКОЕ ВТОРОЕ: INSERT OR IGNORE подавляет ЛЮБОЕ ограничение, а не только совпадение идентичности — у сниппетов уникален ещё и hash, у памяти type под CHECK. Запись со свежим uuid, чей код уже существовал, отбрасывалась молча. А счётчик считал ПРОЧИТАННЫЕ ФАЙЛЫ, не вставки, — то есть восстановление отчитывалось об успехе ровно тогда, когда теряло данные. Цель конфликта названа явно; всё прочее поднимает ошибку с именем файла; счётчик считает вставки; уже существующие строки сообщаются ОТДЕЛЬНО, потому что «оставлено как есть» и «восстановлено» — разные исходы, и только один означает, что бэкап применился. СРЕДНИЕ: экспорт ловил только OSError (сбой SQLite давал голый traceback вместо «бэкап НЕПОЛНЫЙ»); нечисловая версия в манифесте давала ValueError вместо внятного отказа; _parse молча пропускал бы списочные значения, которые общий писатель УМЕЕТ писать, — теперь поднимает ошибку вместо тихой потери. ТРИ ТЕСТА БЫЛИ СЛАБЫМИ: сравнение времени файлов прошло бы ложно при разрешении таймера; сверка полей шла с той же константой, которой пользуется код; проверка «база не скопирована» смотрела расширение. Все три переписаны, каждый проверен пробой. Domain: осмысленно вне тестов и проверено ЖИВЫМ CLI после bootstrap. Это единственная страховка для базы, которая копит знание годами и живёт в одном экземпляре; дефект здесь обнаруживается в момент, когда бэкап понадобился, — поэтому и сверка идёт по содержимому, и отказ атомарен. Полный pytest: 6680 passed, 24 skipped, 0 failed, 0 errors (983s). ruff check — All checks passed. ruff format — already formatted. mypy — Success: no issues found in 298 source files. bootstrap --ide all прогнан, drift отсутствует.
