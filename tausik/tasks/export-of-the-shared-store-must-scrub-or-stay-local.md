---
slug: export-of-the-shared-store-must-scrub-or-stay-local
title: "БЛОКЕР kb-export-global: обоснование «скраббер не нужен» опирается на то, что база не покидает машину, а сама выгрузка это опровергает"
status: done
epic: null
story: null
complexity: medium
role: architect
stack: python
tier: moderate
call_budget: 45
defect_of: null
scope: "Только решение и правка критериев соседней задачи. Кода не трогает: развилка решается ЗАПИСЬЮ, а реализация запрета назначений принадлежит kb-export-global."
scope_exclude: null
relevant_files:
  - "tausik/decisions"
  - "tausik/tasks/kb-export-global.md"
scope_paths: []
scope_tools: []
depends_on: []
completed_at: "2026-08-02T17:38:38Z"
---

## Goal

Найдено ревью безопасности сессии #155, и находка бьёт в моё же решение из kb-global-write.

ПРОТИВОРЕЧИЕ. В scripts/knowledge_write.py записано обоснование: скраббер на пути --global не вызывается, потому что «это файл в домашней папке того же пользователя, та же зона доверия, ничего не пересекает границу». Оно ОПРОВЕРГАЕТСЯ СЛЕДУЮЩЕЙ ЖЕ ЗАДАЧЕЙ ЭТОГО ЭПИКА: kb-export-global в AC1 разрешает бэкап на «произвольный remote (каталог, том, S3-совместимый)». S3-совместимый remote — внешнее хранилище, то есть граница машины пересекается. Ни один байт содержимого никогда не проходил через вычистку, потому что решение на записи постановило, что вычищать нечего.

УСУГУБЛЯЕТ: origin_project хранит АБСОЛЮТНЫЙ путь проекта. В этом самом репозитории он равен D:\\Work\\Kibertum\\clients\\<заказчик>\\..., то есть имя заказчика в открытом виде, у КАЖДОЙ записи КАЖДОГО типа. Плюс source_file у сниппетов может быть абсолютным, если detect звали с абсолютным --path.

РАЗВИЛКА, решить ЯВНО и записать решением, а не выбрать молча:
(а) выгрузка ОБЯЗАНА вычищать — скраббер переезжает на границу выгрузки, и это становится критерием приёмки kb-export-global;
(б) выгрузка ограничивается локальными назначениями (каталог, том), а S3-совместимые и сетевые remote запрещаются до появления вычистки на выгрузке.
Рекомендация: (б) для 1.8 как более дешёвая и fail-closed, (а) следом.

НЕЛЬЗЯ: оставить как есть и надеяться, что до выгрузки кто-нибудь вспомнит. Ровно этот механизм — «мы же помним» — в проекте уже отказывал несколько раз.

## Acceptance Criteria

1. Развилка РЕШЕНА и записана решением, а не выбрана молча: сказано, какой исход принят и ПОЧЕМУ, с опорой на замеры кода, а не на предпочтение.
2. Обоснование опирается на проверяемые факты о существующем механизме: что он делает на самом деле, применим ли он к задаче, и во сколько обходится каждый исход.
3. Критерии приёмки kb-export-global приведены в соответствие с решением, а расхождения между ними и реальностью схемы устранены.
4. НЕГАТИВНЫЙ СЦЕНАРИЙ ЗАФИКСИРОВАН В КРИТЕРИЯХ ВЫГРУЗКИ: недоступное или запрещённое назначение отвергается ГРОМКО, без молчаливого «бэкап не сделан». Это то, ради чего задача заводилась: тихий отказ бэкапа неотличим от его отсутствия.
5. То, что НЕ делается в 1.8, названо явно и вынесено задачей, а не забыто.

## Plan

## Rollback

Откат — записью, а не git revert: решение #219 отменяется новым решением, называющим причину смены исхода, а критерии kb-export-global возвращаются к прежней формулировке. Ничего не развёрнуто и ничего не работает по этому решению, поэтому отката кода не требуется; ровно поэтому развилку и решают ДО реализации.

## Journal

- 2026-08-02T17:31:14Z [planning] — РАЗВЕДКА ПЕРЕД РЕШЕНИЕМ #219 (сессия #156, агент-исследователь). Замеры, определившие исход, и два дефекта в критериях соседней задачи. ЗАМЕР 1 — МЕХАНИЗМА ВЫЧИСТКИ НЕ СУЩЕСТВУЕТ. scripts/brain_scrubbing.py называется scrub, но НИЧЕГО НЕ ВЫЧИЩАЕТ. Публичные функции scrub(content: str) и scrub_with_config возвращают {"ok": bool, "issues": [{detector, severity, match, hint}]} — то есть список НАРУШЕНИЙ, а не изменённый текст. Функции замены или маскирования в модуле нет вовсе. Русское «вычистка» и английское scrub здесь разошлись: фактическая семантика — REFUSE, а не REDACT. Четыре детектора (filesystem_paths, emails, private_urls, project_names_blocklist), все severity=block. ЗАМЕР 2 — ПРИМЕНИТЬ ЕГО К ВЫГРУЗКЕ КАК ЕСТЬ = ГАРАНТИРОВАННЫЙ ОТКАЗ, а не пограничный случай. Детектор _WINDOWS_PATH ловит D:\Work\... без вариантов, а origin_project несёт абсолютный корень проекта у КАЖДОЙ строки КАЖДОЙ из трёх таблиц. Значит блокировка срабатывала бы всегда, бэкап не делался бы НИКОГДА — прямое противоречие цели задачи kb-export-global («единственная точка отказа для всей накопленной за годы памяти») и её же критерию про громкий отказ. ЗАМЕР 3 — SQLITE ОН НЕ УМЕЕТ ВООБЩЕ. stdlib-only, импортирует html/re/unicodedata/urllib.parse; ни sqlite3, ни файлового I/O, докстринг прямо: «Pure function: no I/O». Подать ему .db нельзя даже теоретически: бинарь не декодируется в str, а при насильственном декодировании детекторы срабатывали бы на мусор из страниц и B-tree, включая УДАЛЁННЫЕ, но не затёртые записи во freelist. ЗАМЕР 4 — ПОБАЙТОВАЯ КОПИЯ И ВЫЧИСТКА НЕСОВМЕСТИМЫ, И ЛОГИЧЕСКАЯ ВЫГРУЗКА УЖЕ ТРЕБУЕТСЯ ДВУМЯ СУЩЕСТВУЮЩИМИ КРИТЕРИЯМИ. .db не читается глазами (критерий про «читаем глазами») и не байт-стабилен при одинаковом содержимом из-за freelist, счётчика изменений в заголовке и чекпойнтов WAL (критерий про идемпотентность). Отдельно: FTS5-индексы внешне-контентные, их теневые таблицы несут ТОКЕНИЗИРОВАННЫЕ КОПИИ тех же чувствительных строк, и побайтовая копия выносит их тоже. ЗАМЕР 5 — ВЫЧИСТКА КОНФЛИКТУЕТ С ВОССТАНОВЛЕНИЕМ ДВАЖДЫ. (а) origin_project — структурное поле, редактированием текста не лечится, а его подмена ломает критерий «содержимое совпадает». (б) snippets.hash есть sha256 ОТ ИСХОДНОГО кода и UNIQUE: вычистишь code — либо хэш перестаёт соответствовать содержимому, либо остаётся ОРАКУЛОМ, по которому короткий сниппет перебирается. ОБЪЁМ, ПО КОДУ А НЕ НА ГЛАЗ: исход (б) — около 8-15 вызовов, один предикат на входном аргументе по образцу state_serialize.assert_export_target (26 строк), 3-5 тестов, аддитивно, НЕ ЛОМАЕТ НИ ОДНОГО существующего теста. Исход (а) — 40-80+ вызовов и это только ПОСЛЕ разрешения двух конфликтов критериев, потому что требует построить редактирующий скраббер (~150-250 строк), найти источник блоклиста заказчиков (brain_project_registry — реестр brain-проектов, а не заказчиков) и переписать историю решения kb-global-write про «скраббер не вызывается». Соотношение примерно 1 к 5 и РАЗНОЙ ПРИРОДЫ. ДВА ДЕФЕКТА В КРИТЕРИЯХ kb-export-global, найдены разведкой и ИСПРАВЛЕНЫ: (1) критерий про восстановление требовал сохранности memory_edges, а такой таблицы в схеме общей базы НЕТ ВООБЩЕ — критерий был непроверяем как написан; заменён на сверку трёх таблиц по entry_uuid, который ровно для этого и заведён. (2) критерий про назначение допускал «S3-совместимый» — снято решением #219. ПЛЮС добавлено явное требование логической выгрузки, которое раньше подразумевалось двумя критериями, но не было названо. ПОБОЧНО: scope_paths карточки kb-export-global указывает на scripts/service_export.py, которого В ДЕРЕВЕ НЕТ. Это устаревшая подсказка плана, уже разоблачённая в журналах двух других задач; фактическая реализация подобного лежит в scripts/state_export.py.
- 2026-08-02T17:38:19Z [implementation] — AC verified: 1. ✓ РАЗВИЛКА РЕШЕНА И ЗАПИСАНА. Решение #219: выгрузка общей базы ограничивается ЛОКАЛЬНЫМИ назначениями, сетевые (s3://, http(s)://, UNC) отвергаются fail-closed; вычистка выносится в 1.9. Записано решением, а не выбрано молча — ровно то, отсутствие чего было признано процессным дефектом в этой же серии сессий. 2. ✓ ОБОСНОВАНИЕ НА ЗАМЕРАХ, А НЕ НА ПРЕДПОЧТЕНИИ. Пять проверяемых фактов, все из чтения кода: (а) brain_scrubbing НИЧЕГО НЕ ВЫЧИЩАЕТ — возвращает {ok, issues} и ни байта изменённого текста, семантика refuse, а не redact; (б) применить его как есть = ГАРАНТИРОВАННЫЙ отказ, потому что origin_project несёт абсолютный путь у КАЖДОЙ строки трёх таблиц, а детектор windows-путей ловит его безусловно — бэкап не делался бы никогда, против самой цели задачи; (в) SQLite он не умеет вовсе (pure function, без I/O); (г) побайтовая копия и вычистка несовместимы, и логическая выгрузка УЖЕ требуется двумя существующими критериями; (д) вычистка конфликтует с восстановлением дважды — origin_project и hash сниппета как оракул. ОБЪЁМ посчитан по коду: (б) 8-15 вызовов, аддитивно, не ломает ни одного теста, образец валидации уже есть (state_serialize.assert_export_target); (а) 40-80+ ПЛЮС построить редактирующий скраббер с нуля. 3. ✓ КРИТЕРИИ kb-export-global ПРИВЕДЕНЫ В СООТВЕТСТВИЕ, и заодно исправлены ДВА расхождения со схемой, найденные разведкой: (1) критерий восстановления требовал сохранности memory_edges, а такой таблицы в схеме общей базы НЕТ ВООБЩЕ — он был непроверяем как написан, заменён на сверку трёх таблиц по entry_uuid; (2) снята формулировка «S3-совместимый». ДОБАВЛЕНО явное требование логической выгрузки — оно подразумевалось двумя критериями, но названо не было. 4. ✓ НЕГАТИВНЫЙ СЦЕНАРИЙ В КРИТЕРИЯХ ВЫГРУЗКИ: недоступное или запрещённое назначение отвергается ГРОМКО, и тест обязан проверять ОБЕ половины — что команда падает И что молчаливого успеха нет. Это и есть суть задачи: тихий отказ бэкапа неотличим от отсутствия бэкапа, а обнаруживается в момент, когда бэкап понадобился. 5. ✓ ЧТО НЕ ДЕЛАЕТСЯ В 1.8 — НАЗВАНО. Вычистка на выгрузке вынесена в 1.9; сказано, что для неё придётся ПОСТРОИТЬ редактирующий механизм, а не переставить существующий вызов, и что предварительно надо разрешить конфликт с критерием «содержимое совпадает». ЧЕСТНО ПРО ВЕРИФИКАЦИЮ: verify пройден с --no-tests-expected, и сам инструмент это отметил — «this closure rests on a declaration, not on a verification». Так и есть: задача архитектурная, кода не меняет, проверять запуском нечего. Область объявлена свободным текстом именно поэтому. Проверяемость решения перенесена в kb-export-global, где запрет назначений станет кодом и тестами; там же проба фальсифицируемости. ROLLBACK: откат записью, а не git revert — решение #219 отменяется новым решением, называющим причину смены исхода. Ничего не развёрнуто, поэтому отката кода не требуется; ровно поэтому развилку и решают ДО реализации, а не после. Domain: осмысленно вне тестов. Речь о реальном файле в домашней папке, который планировалось копировать наружу, и о реальном содержимом — путях с именами заказчиков у каждой записи. Проверка «применим ли скраббер» сделана чтением его кода и его единственной точки вызова, а не предположением о том, что делает функция с таким именем.
- 2026-08-02T17:38:59Z [done] — ЧЕК-ЛИСТ ВЕРИФИКАЦИИ (SENAR Rule 5). Задача архитектурная: кода не меняет, проверять запуском нечего, поэтому ссылок на тесты здесь быть НЕ МОЖЕТ — и это не пробел, а свойство задачи. AC-1 (развилка решена и записана): ✓ решение #219 в базе, привязано к этой задаче. Проверяемо чтением: tausik decisions. AC-2 (обоснование на замерах): ✓ пять фактов, каждый — ссылка на конкретный код: scripts/brain_scrubbing.py:305-346 (возвращает {ok, issues}, изменённого текста нет); :36 _WINDOWS_PATH против scripts/knowledge_write.py:61-70 origin_project_root (абсолютный путь у каждой строки); докстринг brain_scrubbing:4 «Pure function: no I/O»; scripts/knowledge_db.py:104-115 (внешне-контентные FTS с токенизированными копиями); scripts/knowledge_write.py:170 (hash от исходного кода, UNIQUE). AC-3 (критерии приведены в соответствие): ✓ tausik/tasks/kb-export-global.md, критерии 1-3 переписаны; проверяемо диффом карточки. AC-4 (негативный сценарий в критериях выгрузки): ✓ критерий 6 kb-export-global требует проверки ОБЕИХ половин. AC-5 (что не делается — названо): ✓ вычистка на выгрузке вынесена в 1.9 с указанием, что её механизм придётся ПОСТРОИТЬ. ЗЕЛЁНЫЙ VERIFICATION_RUN: #1630, exit=0, подписан (key 103a83a2). Он честно помечен no_tests_declared=1, и инструмент сам сказал: «this closure rests on a declaration, not on a verification». Так и есть. Проверяемость перенесена в kb-export-global, где запрет назначений станет кодом, тестами и пробой фальсифицируемости. CHANGELOG: закрыто с --no-changelog осознанно. Решение НИЧЕГО не меняет для пользователя сегодня — ничего не реализовано. Запись в CHANGELOG появится вместе с kb-export-global, где запрет станет наблюдаемым поведением. Анонсировать намерение как изменение значило бы сделать ровно то, за что эта серия задач и штрафует: написать шире сделанного.
