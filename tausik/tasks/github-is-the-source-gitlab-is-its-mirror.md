---
slug: github-is-the-source-gitlab-is-its-mirror
title: "Инверсия направления публикации: сегодня истории GitHub и GitLab не имеют общего предка"
status: active
epic: release-19-renar-conformance
story: github-primary-gitlab-mirror
complexity: null
role: architect
stack: null
tier: substantial
call_budget: 150
defect_of: null
scope: null
scope_exclude: null
relevant_files: []
scope_paths:
  - CHANGELOG.md
  - CHANGELOG.ru.md
  - "scripts/*.py"
  - "tests/*.py"
  - "docs/ru/*.md"
  - "docs/en/*.md"
  - "bootstrap/*.py"
  - ".gitignore"
scope_tools: []
depends_on: []
completed_at: null
---

## Goal

ЗАМЕР, сессия #178: origin (GitLab) содержит 405 коммитов и 3403 файла; github/main — 13 коммитов и 1069 файлов; git merge-base между ними пуст, то есть это ORPHAN-снимок, а не ветка общей истории. Публикация сегодня есть вычищенный снимок, накатываемый вручную, и потому GitHub систематически отстаёт — тикеты #7 и #8 заведены внешним автором против коммита 623fb4e.

ТРЕБОВАНИЕ ВЛАДЕЛЬЦА: наружу уезжает только фреймворк; GitLab становится ЗЕРКАЛОМ GitHub. Это инверсия: источником истины становится публичный репозиторий.

ГЛАВНЫЙ ВОПРОС, КОТОРЫЙ ЗАДАЧА ОБЯЗАНА ЗАДАТЬ ДО ЛЮБОЙ РАБОТЫ, а не решить молча: публикуется ли история из 405 коммитов, или публичная история начинается с нуля и приватная остаётся архивом? Первое требует полного аудита утечек по ВСЕЙ истории, а не по снимку — прежний аудит проверял именно снимок. Второе дешевле и честнее, но теряет происхождение, а происхождение мы продаём.

ЧТО ВЫЯСНЕНО И НЕ ТРЕБУЕТ РАБОТЫ: сайт из ядра уже вынесен, tausik/site есть отдельный репозиторий, остатков в core нет (проверено ls и поиском по tausik.tech). Разделение репозиториев в этой части выполнено.

ЧТО ОСТАЁТСЯ ПОМИМО ГЛАВНОГО ВОПРОСА: чем определяется, какие 1069 файлов из 3403 уезжают наружу — это правило сейчас нигде не записано как проверяемое; куда девается проекция tausik/ (сегодня на публичном зеркале лежит ровно один файл gates.json); переезжает ли сайт на GitHub тоже или остаётся только в GitLab. НЕГАТИВНОЕ: до ответа на главный вопрос не трогать remote и не переписывать историю.

## Acceptance Criteria

AC1. ГЛАВНЫЙ ВОПРОС ОТВЕЧЕН ВЛАДЕЛЬЦЕМ, решение #257: публичная история начинается ВПЕРЁД от текущей точки, 405 коммитов остаются приватным архивом на GitLab, историю НЕ переписываем. Проекция tausik/ ПУБЛИКУЕТСЯ. Три измеренные причины отказа от переписывания записаны в решении.
AC2. БАЗА ВЫЧИЩЕНА ПЕРВОЙ, а не проекция. Строки живут в .tausik/tausik.db и регенерируются в дерево первым же task log — правка одной проекции есть работа, которую фреймворк сам отменит. Вычищены: адрес внутреннего хоста, имена чужих проектов, локальные пути с именем пользователя.
AC3. ПРОЕКЦИЯ ПЕРЕСОБРАНА ИЗ ВЫЧИЩЕННОЙ БАЗЫ (state export), а не отредактирована руками. Ручная правка проекции запрещена: она разойдётся с базой при следующем экспорте.
AC4. НЕГАТИВНЫЙ СЦЕНАРИЙ: скан прогоняется ПОВТОРНО после чистки и обязан дать ноль по всем четырём классам. Скан, прогнанный только до чистки, ничего не доказывает о том, что уезжает.
AC5. НЕГАТИВНЫЙ СЦЕНАРИЙ: публикационный коммит строится ПОВЕРХ существующего GitHub main, а не вместо него. Проверяется тем, что 623fb4e остаётся достижимым, 22 тега не двигаются, PR #5 остаётся mergeable.
AC6. PUSH НЕ ДЕЛАЕТСЯ, пока владелец не увидел точный список команд и результат сухого прогона. Публикация необратима.
AC7. Правило 'что уезжает наружу' записано проверяемо. После решения #257 оно простое — всё, что в git, — и потому обязано быть закреплено тестом или гейтом, а не подразумеваться.

## Plan

## Rollback

git revert публикационного коммита; push не делался до явного согласия владельца, поэтому откат локальный. Вычеркивание в БД необратимо по решению #258 — путь назад только через бэкапы d:/tmp/tausik-db-backups/ (s180 и s181).

## Journal

- 2026-08-25T14:35:54Z [implementation] — AC1 ✓ — главный вопрос отвечён владельцем, решение #257: чистый лист вперёд, историю не переписываем, проекция публикуется. AC2 ЗАБЛОКИРОВАН ИЗМЕРЕННОЙ НЕВОЗМОЖНОСТЬЮ, а не отложен по усмотрению. Из 69 утекающих строк CLI достаёт 23 (Goal, Acceptance Criteria, Plan, frontmatter — через task update). Остальные 46 недоступны: 27 в журналах задач (task_logs append-only, команды правки нет), 15 в теле памяти (memory: add и delete, update отсутствует), 4 в решениях (decisions: только list с --limit). Команд redact, scrub, sanitize, rewrite, edit в CLI нет. Заведена задача nothing-can-redact-the-memory-the-framework-publishes. Разбивка утечек по секциям: tasks/Journal 27, tasks/Acceptance Criteria 13, memory/body 10, tasks/Goal 6, memory/заголовок 5, decisions/Rationale 3, tasks/Plan 2, tasks/frontmatter 2, decisions/Decision 1. СНЯТАЯ ТРЕВОГА: Frai и KAI — прежние имена САМОГО TAUSIK (решения о ребрендинге), а не чужой проект. Из списка вычеркиваемых имён убраны. Метод перечисления при этом показал дырявость: имя нашлось случайно, а не проверкой. ДЕФЕКТ CLI, НАЙДЕННЫЙ ПОПУТНО: подсказка в отказе task add учит неверному вызову — печатает 'tausik task add <story-slug> <task-slug> "Title"' (три позиционных), тогда как реальная сигнатура одна позиционная плюс --story и --slug. Отказ, который учит неправильному, хуже отказа без примера.
- 2026-08-25T15:19:22Z — AC2 ✓ РАЗБЛОКИРОВАН И ВЫПОЛНЕН: задача nothing-can-redact-the-memory-the-framework-publishes закрыта, механизм вычеркивания построен и применён. Вычеркнуто 92 совпадения в 61 колонке базы: internal-host 46, third-party-project 31, local-path 15. AC3 ✓ проекция ПЕРЕСОБРАНА из вычеркнутой базы командой state export — 2417 файлов, руками не правилось ни одного. AC4 ✓ ПОВТОРНЫЙ СКАН после чистки: все семь образцов дают ноль по всему дереву tausik/, секреты ноль. В дереве 72 видимых маркера вычеркивания, в базе 61 запись следа с основанием и временем.
- 2026-08-25T15:44:10Z [implementation] — AC4 ПЕРЕПРОВЕРЕН И ОКАЗАЛСЯ УЗКИМ. Скан #180 дал ноль по семи образцам, и это правда; но образцов было семь, а классов три, и по классам дерево НЕ чисто. Замер сессии #181 по расширенному набору: 1. ОТЧЁТ О ВЫЧЕРКИВАНИИ САМ ВЕРНУЛ УТЕЧКУ. В журнале задачи nothing-can-redact-the-memory-the-framework-publishes (запись 2026-08-25T15:18:19Z, AC-9) записано дословно: «Повторный скан проекции: gitlab-хост 0, <четыре имени третьих лиц> 0, имя пользователя 0, секреты 0». Чтобы отчитаться о чистоте, запись ПЕРЕЧИСЛИЛА то, что вычеркнула. Четыре имени третьих лиц вернулись в дерево ровно тем предложением, которое доказывало их отсутствие. Скан не мог этого увидеть: он прогонялся ДО того, как отчёт был написан. Это не оплошность, а класс. Докстринг redact_scope.marker предсказал его буквально: «маркер, цитирующий значение, положил бы утечку обратно в ту колонку, из которой её убрали». Предсказание относилось к маркеру, а нарушил его ОТЧЁТ. Правило шире, чем место, где оно записано: ЛЮБОЙ текст, называющий вычеркнутое, есть новая утечка — включая доказательство того, что вычеркивание сработало. 2. ЛОКАЛЬНЫЙ ПУТЬ ПЕРЕЖИЛ ЧИСТКУ В ЧЕТЫРЁХ МЕСТАХ проекции: путь вида D:/Work/<...>/claude в tausik/memory/multiple-stale-mcp-project-servers-*.md, tausik/tasks/mem-posttool-markers.md (двумя записями) и tausik/tasks/v155-kilo-python-portable-gitignore.md. Образец local-path из #180 ловил другое написание. Тупик #180 «список имён дыряв» подтверждён ТРЕТИЙ раз, и впервые — живыми выжившими, а не гипотезой. 3. СКАН ПРОВЕРЯЛ НЕ ТО ДЕРЕВО. AC4 сформулирован как «ноль по всему дереву tausik/», а публикуется по решению #257 ВЕСЬ трекаемый репозиторий — 3501 файл, из них 1083 вне tausik/. Вне проекции найдено: настоящее имя пользователя в обоих CHANGELOG (по два вхождения), в scripts/project_config.py:171-172, scripts/supply_eol.py:49-50, tests/test_the_home_tier_is_not_a_project.py:125 — в обоих написаниях, коротком 8.3 и длинном. Эти строки redact не достаёт вовсе: они живут файлами, а не колонками БД, то есть у механизма вычеркивания есть измеренная граница, и она проходит ровно по краю публикуемого дерева. Проверенные вхождения-пустышки, правки не требующие: C:\Users\<user>, C:\Users\<u>, C:\Users\me\proj, C:\Users\x — обобщённые заполнители; D:/Work/repo, D:/Work/clientA/repo, D:/Work/Kibertum/clients/acme/repo в тестах — синтетические фикстуры. ВЫВОД ДЛЯ ПОРЯДКА РАБОТ: AC5 (публикационный коммит) НЕ строится, пока это не починено — коммит зафиксирует дерево, и утечка уедет вместе с ним. Порядок: (а) вычеркнуть найденное из БД и пересобрать проекцию, (б) починить файловые вхождения вне tausik/, (в) AC7 закрыть гейтом, который читает ВЕСЬ трекаемый список git и опознаёт ФОРМЫ, а не перечень имён, (г) только затем AC5.
- 2026-08-25T15:57:48Z [implementation] — ВТОРОЙ ПРОХОД ЧИСТКИ ВЫПОЛНЕН, И МЕТОД СМЕНЁН С ПЕРЕЧНЯ НА ФОРМУ. Вычеркнуто в этой сессии, всего 24 совпадения поверх 92 из #180: - 8 совпадений, четыре имени третьих лиц, вернувшихся в дерево ОТЧЁТОМ о вычеркивании (task_logs.message и tasks.notes, по паре на имя); - 6 совпадений локального пути в написании, которого не было в образцах #180 (memory.content, task_logs.message, tasks.acceptance_criteria, tasks.notes); - 10 совпадений, найденных уже сканом ФОРМЫ, а не перечнем: decisions.rationale 1, task_logs.message 3, tasks.acceptance_criteria 2, tasks.notes 3, tasks.scope 1. Среди них полный путь рабочего каталога репозитория и путь второго репозитория владельца. Проекция пересобрана из БД дважды (2419 файлов). Маркеров в дереве стало 87: internal-host 34, third-party-project 32, local-path 21. ПОЧИНЕНО ФАЙЛАМИ (redact туда не достаёт, это колонки не БД, а исходники): - настоящее имя учётной записи Windows в обоих написаниях (короткое 8.3 и длинное) — 9 вхождений в CHANGELOG.md, CHANGELOG.ru.md, scripts/project_config.py, scripts/supply_eol.py, tests/test_the_home_tier_is_not_a_project.py. Заменено обобщённой парой, которая усекается по тому же правилу 8.3, поэтому иллюстрация осталась арифметически честной. - имя клиентского проекта и локальный путь в ТЕСТОВЫХ ФИКСТУРАХ — 14 вхождений в tests/test_brain_scrubbing.py, tests/test_brain_mcp_write.py, tests/test_memory_markers.py. Фикстура публична ровно настолько же, насколько файл, который её держит. Правка велась ПОБАЙТОВО: read_text/write_text нормализовали бы CRLF в LF и превратили правку девяти токенов в переписывание файлов целиком. Диф — 8 строк на 5 файлов. tests/test_memory_markers.py, test_brain_scrubbing.py, test_brain_mcp_write.py: 143 passed. ТРЕТЬЕ ПОДТВЕРЖДЕНИЕ ДЫРЯВОСТИ ПЕРЕЧНЯ, теперь с точной механикой: мой первый скан был РЕГИСТРОЗАВИСИМЫМ и дал ноль по имени клиента, тогда как в прозе оно написано с большой буквы, а в конфиге — с маленькой. Ноль был ложным. Тесты покраснели ровно на этом: блок-лист уже нёс новое имя, а текст рядом — старое. Правило: для класса «имя» скан обязан быть регистронезависимым, для класса «путь» — по форме, а не по перечню. ДЕФЕКТ CLI, НАЙДЕННЫЙ ПОПУТНО: redact --regex с неверным регулярным выражением падает СЫРЫМ ТРЕЙСБЕКОМ из re.compile, а не именованным отказом. Команда, которая для нулевого совпадения имеет отдельную формулировку (её собственный AC7), для неверного шаблона не имеет никакой. Заведено отдельной задачей. ОСТАЛОСЬ НА РЕШЕНИЕ ВЛАДЕЛЬЦА, ВЫНОСИТСЯ НА ЧЕКПОЙНТ AC6 — не чиню молча, потому что это его личный материал, а публикация необратима: Память проекта (запись «внутреннее исследование живёт в gitignored docs/research») сама постановляет: исследование с личными данными — профили закладок, нерелизные кодовые имена, конкурентная стратегия — уходит в gitignored-сток и НЕ публикуется. Замер показывает, что правило нарушено в самом git: 1. docs/ru/research/bookmark-corpus-review-2026-08.md — 127 строк, трекается; это разбор трёхлетнего корпуса закладок владельца, то есть ровно «профиль закладок» из запрета. 2. Кодовое имя нерелизного направления — 21 вхождение, включая ИМЯ ФАЙЛА задачи в проекции. Та же запись памяти прямо предупреждала, что имя файла само по себе течёт, и приводила в пример этот самый слаг. 3. Имя стороннего сервиса закладок — 34 вхождения в десяти файлах. 4. СТРУКТУРНЫЙ ПРОБЕЛ, чиню сам, решения не требует: сток .gitignore записан как docs/research/_internal/, а исследования лежат в ТРЁХ деревьях — docs/research/, docs/ru/research/, docs/en/research/. Правило покрывает одно из трёх, поэтому переложить файл в сток внутри локализованного дерева сегодня невозможно: он всё равно останется трекаемым.
- 2026-08-25T16:05:27Z [implementation] — AC7 ✓ И AC5 ✓. AC6 ПОДГОТОВЛЕН И УПИРАЕТСЯ В ОДНО ИЗМЕРЕННОЕ ПРОТИВОРЕЧИЕ, названное ниже. AC7 — правило записано проверяемо. scripts/publication_scope.py: published_paths спрашивает git и отдаёт ответ НЕИЗМЕНЁННЫМ; параметра фильтра у функции нет намеренно, и докстринг говорит почему — добавление фильтра и есть та правка, ради предотвращения которой модуль существует. Граница «публикуется / остаётся дома» отдана .gitignore и больше никому. Рядом три проверки самого акта публикации: дерево уезжает целиком, прежняя публичная голова остаётся достижимой, ни один тег не двигается. Три функции, а не одна verify(), чтобы вызывающий знал КАКОЕ обещание сломалось — единственный булев ответ оставил бы оператора гадать ровно в тот момент, когда гадать дороже всего. Ошибка git поднимает PublicationError, а не возвращает пустой кортеж: пустой ответ от упавшего вызова выглядел бы как «публиковать нечего» и прошёл бы все проверки, ничего не проверив (тот же класс, что check-result-conflates-could-not-run-with-passed). tests/test_publication_scope.py — 14 тестов, и у КАЖДОГО обещания есть парный красный случай: потерянный файл назван поимённо, а не сосчитан; orphan-коммит с тем же деревом отвергнут (ровно ошибка публикации 2026 года, на которую тогда никто не возразил); сдвинутый тег и ИСЧЕЗНУВШИЙ тег пойманы порознь — проверка, сравнивающая только общие ключи, назвала бы исчезновение чистым; каталог, не являющийся репозиторием, поднимает исключение. Прогон 14 passed. СТРАЖ ПРОВЕРЕН МУТАЦИЕЙ, а не объявлен: внесённый фильтр «не публиковать tausik/» роняет два теста; файл восстановлен побайтово. Без мутации утверждение «правило под стражей» было бы того же сорта, что три вырожденных контроля из памяти #404. AC5 — публикационный коммит построен ПОВЕРХ публичной головы. base = кэшированный refs/remotes/github/main = 623fb4e; коммит 16764ec, лежит под локальной ссылкой refs/publication/main. Построен через commit-tree, а не checkout+add+commit: акт над объектами не может потерять незакоммиченную правку и не трогает ни рабочее дерево, ни индекс, ни текущую ветку. Проверено сразу после построения: [PASS] дерево публикации побайтово совпадает с деревом HEAD; [PASS] 623fb4e остаётся предком 16764ec; [PASS] все 22 тега называют те же объекты. REMOTE НЕ ТРОГАЛСЯ НИ РАЗУ. МАСШТАБ: 3506 трекаемых путей; относительно 623fb4e — 2437 добавлений, 47 изменений и НОЛЬ УДАЛЕНИЙ. Ни один файл, который сегодня есть у публичного потребителя, не исчезает. ГРАНИЦА ЭТОГО ЗАМЕРА НАЗВАНА ВСЛУХ: base взят из КЭШИРОВАННОЙ ссылки. Подтвердить, что публичная голова не сдвинулась, можно только fetch-ем, а fetch — обращение к remote. Поэтому git fetch github стоит ПЕРВОЙ командой в списке AC6, и пересборка коммита после него — то, что делает base достоверным. AC6, ПРОТИВОРЕЧИЕ, КОТОРОЕ Я НЕ ИМЕЮ ПРАВА ЗАМОЛЧАТЬ: третья обязательная проверка — «PR #5 остаётся MERGEABLE» — по устройству не может быть обещана. Публикация меняет 47 файлов, и среди них bootstrap/bootstrap_hooks.py — ИМЕННО ТОТ файл, который PR #5 переписывает своим якорением матчеров. GitHub пересчитывает mergeable от новой базы, и с высокой вероятностью PR перейдёт из MERGEABLE в CONFLICTING. Проверить это до push нельзя: нужен fetch ветки pull/5/head, то есть снова обращение к remote. Как это читать. Требование AC6 писалось против ПЕРЕПИСЫВАНИЯ ИСТОРИИ, которое оторвало бы PR от базы совсем — и от этого мы защищены: 623fb4e остаётся предком, теги не двигаются, ничего не удаляется. Конфликт содержимого — вещь другая и меньшая: PR остаётся открытым, авторство сохраняется, автор может перебазировать. Но поле GitHub всё равно сменит значение, и обещать «остаётся MERGEABLE» дословно я не могу. Смягчение уже принято проектом, изобретать нечего: конвенция #382 — зеркало не имеет права стереть авторство вклада снаружи; AC4 задачи port-external-pr5-hook-coverage — автор указывается соавтором коммита, а PR закрывается ССЫЛКОЙ НА ПЕРЕНОС, а не молча. Разбор PR #5 уже лежит в журнале той задачи полностью. Вопрос владельцу на чекпойнте: переносим находку и закрываем PR ссылкой ДО публикации или ПОСЛЕ.
