---
slug: revise-kb-export-tasks-superseded
title: "Ревизия: kb-export-tasks и часть kb-brain-deprecate, похоже, уже выполнены другими эпиками — проверить и списать"
status: done
epic: shared-knowledge
story: kb-git-sync
complexity: simple
role: architect
stack: python
tier: light
call_budget: 20
defect_of: null
scope: "Карточки kb-export-tasks и kb-brain-deprecate в проектной БД (goal, acceptance_criteria, call_budget, status) и их проекция в tausik/tasks/*.md. Чтение артефактов реализации: scripts/service_export.py, scripts/state_serialize.py, scripts/service_knowledge.py, harness/claude/mcp/brain/, tests/ — ТОЛЬКО чтение."
scope_exclude: "Код не изменяется: scripts/, harness/, tests/, docs/, bootstrap/ — read-only. CHANGELOG не трогаем (ревизия оценки не является пользовательским изменением). Не переоткрываем и не переписываем карточки за пределами двух названных."
relevant_files:
  - CLAUDE.md
  - "tausik/stories/kb-git-sync.md"
  - "tausik/tasks/kb-brain-deprecate.md"
  - "tausik/tasks/kb-export-tasks.md"
  - "tausik/tasks/revise-kb-export-tasks-superseded.md"
  - "tausik/decisions/reviziya-obema-1-8-sokratila-byudzhet-s-2087-do-1927-no-ne.md"
  - "tausik/tasks/cascade-mutations-never-project-to-tree.md"
  - "tausik/tasks/publish-risk-gate-docstring-lies-after-205.md"
  - "tausik/tasks/tausik-tree-gitattributes-lf.md"
  - "tausik/tasks/tool-call-syntax-leaks-into-entity-text.md"
scope_paths: []
scope_tools: []
depends_on: []
completed_at: "2026-07-28T19:39:53Z"
---

## Goal

Найдено в сессии #152 при подсчёте остатка объёма 1.8 по запросу владельца. Обе находки требуют ПРОВЕРКИ, а не списания на глаз — задача именно про проверку.

(1) kb-export-tasks (бюджет 150). Его цель дословно: «Проектная БД остаётся ИСТИНОЙ, git — канал синхронизации. Начинаем с задач: slug уже уникален и мержится. Формат решает судьбу мержа: ОДИН ФАЙЛ НА СУЩНОСТЬ со стабильным порядком полей — тогда диффы минимальны и конфликт человекочитаем; один большой JSONL превращает…». Это описание того, что РЕАЛИЗОВАНО эпиком team-state-in-git (state export/import, tausik/tasks/<slug>.md, детерминированный порядок ключей, round-trip гейт), закрытым в этом же релизе. Карточка писалась ДО него. Проверить построчно по её критериям приёмки, что именно осталось невыполненным, и либо закрыть как поглощённую, либо сузить до остатка.

(2) kb-brain-deprecate (бюджет 150). Его цель включает «Убрать вредный авто-классификатор: он системно шлёт внутренние решения наружу». Ровно этот дефект закрыт в сессии #152 задачей brain-decide-publishes-unclassified-rationale: классификация теперь идёт по всему публикуемому материалу, решения заведены под гейт риска (#205), побочная публикация из одноразового контекста заблокирована. Проверить, что осталось от карточки за вычетом сделанного (вероятно — только понижение Notion до опционального sync-бэкенда, которое пересекается с brainh-reliability).

ПОЧЕМУ ЭТО ВАЖНО СЕЙЧАС. Владелец спрашивает срок релиза. Оценка остатка (2087 вызовов бюджета) включает эти 300 — если они поглощены, оценка завышена примерно на седьмую часть, и это надо знать до планирования полос, а не после.

НЕ ДЕЛАТЬ: не списывать по совпадению формулировок. Совпадение цели — повод открыть карточку и сверить критерии приёмки, а не вердикт.

## Acceptance Criteria

1. По kb-export-tasks каждый содержательный критерий приёмки сверен с ФАКТИЧЕСКИМ артефактом в дереве (файл, тест или команда с выводом); по каждому в журнал записана строка «критерий N → артефакт → выполнен/не выполнен».
2. По kb-brain-deprecate каждый из пяти содержательных критериев сверен так же и записан в журнал в той же форме.
3. По каждой карточке вынесен вердикт из трёх — поглощена целиком / сужена до остатка / остаётся как есть — с явным указанием поглотившей задачи или эпика. Вердикт опирается на пункты 1-2, а не на совпадение формулировок.
4. Вердикт ПРИМЕНЁН в БД: поглощённое закрыто, суженное переписано (goal, acceptance_criteria, call_budget), и проекция tausik/tasks/*.md совпадает с БД (tausik state export отработал, git diff показывает изменения).
5. Пересчитанный остаток объёма 1.8 — число открытых задач и сумма бюджетов — записан в журнал; если он изменился относительно 2087, расхождение зафиксировано решением tausik decide.
6. НЕГАТИВНЫЙ СЦЕНАРИЙ (граница списания): критерий, для которого артефакт НЕ найден, записывается как «не выполнен» и БЛОКИРУЕТ закрытие карточки — она сужается до остатка, а не закрывается. Ошибка «поглощено по совпадению формулировок» невозможна: вердикт «поглощена» допустим ТОЛЬКО когда артефакт назван для КАЖДОГО критерия. Если артефакт не найден ни для одного критерия — карточка остаётся как есть, бюджет не уменьшается.

## Plan

## Rollback

Ревизия не меняет код. Откат — вернуть статус и поля затронутых карточек (kb-export-tasks, kb-brain-deprecate) через task update + git revert проекции tausik/tasks/.

## Journal

- 2026-07-28T19:27:25Z [implementation] — СВЕРКА КАРТОЧКИ 1 — kb-export-tasks. Каждый критерий против фактического артефакта в дереве, не против формулировок. Критерий 1 (ОДИН ФАЙЛ НА ЗАДАЧУ, markdown+frontmatter, стабильный порядок полей, минимальный дифф) → ВЫПОЛНЕН. Артефакты: живое дерево tausik/tasks/*.md (по файлу на слаг, фронтматтер из 17 ключей); scripts/state_export.py::_task_doc строит пары в фиксированном порядке; scripts/state_serialize.py — рукописный YAML-эмиттер с фиксированным порядком ключей, LF-only, одним завершающим \n. Тесты: tests/test_state_export.py::test_fixed_frontmatter_key_order_for_task (порядок 17 ключей закреплён списком), ::test_export_is_deterministic_across_two_builds, ::test_lf_only_and_single_trailing_newline, ::test_shuffled_row_order_yields_identical_bytes (порядок строк в БД не влияет на байты). Критерий 2 (выгружается замысел — заголовок, цель, критерии, план, scope, rollback, статус, журнал; доказательства НЕ выгружаются) → ВЫПОЛНЕН. Артефакт: state_export.py::_task_doc — тело файла ровно из section("Goal"), section("Acceptance Criteria"), section("Plan"), section("Rollback"), _journal_section; фронтматтер несёт status/scope/scope_exclude/relevant_files/scope_paths/scope_tools. Доказательств нет структурно, а не по недосмотру: state_serialize.ENTITY_DIRS = (epics, stories, tasks, decisions, memory) — таблицы verify_runs/receipts/подписей в проекцию не входят вовсе; grep 'evidence' по state_export.py — ноль совпадений. Тест: tests/test_state_export.py::test_all_durable_fields_serialized_runtime_excluded — durable-поля обязаны присутствовать, а risk_score/attempts/claimed_by/started_at/agent-x обязаны отсутствовать. Критерий 3 (идентификация по slug; повторная выгрузка без изменений не порождает диффа) → ВЫПОЛНЕН. Тесты: tests/test_state_export.py::test_layout_paths_use_slugs, ::test_export_idempotent_write_then_check_clean (write -> check_tree пусто -> повторный write не меняет ни байта), ::test_slugless_decision_refuses_with_migration_hint (сущность без стабильного слага отвергается громко, а не получает эфемерный). Критерий CHANGELOG → ВЫПОЛНЕН поглотившей работой: CHANGELOG.ru.md:1681 «Состояние проекта экспортируется в git-native дерево» + зеркало EN. ВЕРДИКТ КАРТОЧКИ 1: ПОГЛОЩЕНА ЦЕЛИКОМ эпиком team-state-in-git (задачи state-git-spec, state-git-stable-ids, state-git-export, state-git-import, gate_state_roundtrip). Артефакт назван для КАЖДОГО критерия — условие AC6 этой ревизии соблюдено. Остатка нет. Единственное расхождение — scope_paths карточки указывает scripts/service_export.py, файла с таким именем не существует: реализация легла в scripts/state_export.py. Это устаревшая подсказка плана, а не критерий, и на вердикт не влияет.
- 2026-07-28T19:27:52Z [implementation] — СВЕРКА КАРТОЧКИ 2 — kb-brain-deprecate. Пять критериев против фактических артефактов. Критерий 1 (Notion не удалён; опциональный публикатор И двусторонний sync ПОВЕРХ ЛОКАЛЬНОГО KB; при выключенном флаге агентский цикл его не касается) → НЕ ВЫПОЛНЕН, частично. Выполнена только флаговая половина: scripts/brain_runtime.py::open_brain_deps:131 `if not cfg.get("enabled"): return None, None, cfg` — при выключенном флаге ни зеркало, ни клиент не открываются. Не выполнено главное: ЛОКАЛЬНОГО KB, поверх которого Notion должен стать sync-бэкендом, не существует — kb-global-schema, kb-global-write, kb-global-read, kb-global-version-guard, kb-global-promote все в planning (проверено task list --status planning). Двусторонний sync карточка сама делегирует brainh-reliability, тоже planning. Критерий 2 (авто-классификатор УБРАН; видимость определяется явным флагом global, а не текстовыми маркерами) → НЕ ВЫПОЛНЕН, дефект СМЯГЧЁН но механизм на месте. Что сделано в #152 (brain-decide-publishes-unclassified-rationale): классификатор стал получать ВЕСЬ публикуемый материал вместо одного заголовка (brain_runtime.decision_publish_fields — единственный источник нагрузки), decisions заведены под гейт риска (решение #205), публикация из чужой БД закрыта fail-closed. Что НЕ сделано: brain_classifier.classify по-прежнему судит по ТЕКСТОВЫМ МАРКЕРАМ (memory_markers.detect_markers), а флага global у записи нет — он предмет kb-global-write (planning). То есть #152 убрал ложь классификатора о материале, а не сам классификатор. Формулировка критерия — «убран», а не «доработан». Критерий 3 (приватность проверяется ОДИН раз — на границе публикации) → НЕ ВЫПОЛНЕН. Проверок по-прежнему три и в разных слоях: маршрутизация в service_knowledge.decide (classify -> local|brain), гейт риска brain_publish_flow.maybe_block_high_risk_publish (тот же classify, второй прогон), scrub_inputs внутри try_brain_write_decision. #152 добавил к ним четвёртую (_is_working_project_db). Один раз на границе — это целевое состояние после появления флага global, а не текущее. Критерий 4 (MCP-сервер brain и мастер настройки Notion сохранены за флагом в минимальной форме) → ВЫПОЛНЕН частично, «за флагом» есть (см. критерий 1), «в минимальной форме» не проверяемо до понижения — минимум определяется тем, что останется после снятия классификатора. Критерий 5 (разовый импорт накопленных Notion-записей в локальный KB выполнен) → НЕ ВЫПОЛНЕН. Импортировать некуда: локального KB нет (см. критерий 1). ВЕРДИКТ КАРТОЧКИ 2: НЕ ПОГЛОЩЕНА, СУЖАЕТСЯ ДО ОСТАТКА. Артефакт назван не для каждого критерия — по правилу AC6 этой ревизии закрытие запрещено. Из пяти критериев #152 закрыл содержательную часть одного (второго) и то не по букве: маршрутизация перестала врать о материале, но классификатор остался и флага global нет. Сужение: из карточки уходит расследование и постановка дефекта «классификатор шлёт внутреннее наружу» (сделано и закрыто тестами), остаётся понижение как таковое — снятие классификатора после появления флага global, схлопывание проверок приватности в одну на границе, импорт накопленного. Все три упираются в kb-global-write, то есть карточка получает ЖЁСТКУЮ зависимость от полосы B.
- 2026-07-28T19:31:37Z [implementation] — ПЕРЕСЧЁТ ОСТАТКА ОБЪЁМА 1.8 (критерий 5). СНАЧАЛА ВОСПРОИЗВЁЛ ИСХОДНУЮ ЦИФРУ, чтобы пересчёт был сопоставим, а не новым замером по другой методике. 2087 из хендоффа #152 воспроизводится точно при трёх допущениях: задачам без call_budget подставлен потолок тира по complexity (complex→150 substantial, medium→60 moderate, simple→25 light, docs/ru/agent-contract.md:132), kb-export-tasks и kb-brain-deprecate шли по 150, и в счёт НЕ входила lanes-changelog-fragments (50), созданная позже. Сумма по той же методике сегодня до правок = 2137; минус 50 = 2087. Совпадение точное, методика та же. СТАЛО. Открытых в объёме 1.8: 23 (22 shared-knowledge + 1 landscape-2026-h2 release-18-breaking-change-notes). Сумма бюджетов = 1927. Проверено двумя способами: (а) 2137 − 150 (kb-export-tasks закрыта) − 60 (kb-brain-deprecate 150→90) = 1927; (б) прямой суммой — задачи с бюджетом 1032, задачи без бюджета по потолку тира 895. ЧТО ЭТО ЗНАЧИТ ДЛЯ СРОКА — ГЛАВНОЕ И НЕПРИЯТНОЕ. Ожидание из карточки («поглощены ~300 из 2087, оценка завышена примерно на седьмую часть») НЕ ПОДТВЕРДИЛОСЬ. Поглощена одна карточка из двух: −160 бюджета, то есть −7.7%, а не −14%. Вторая не поглощена и получила ЖЁСТКУЮ зависимость от полосы B, то есть стала не дешевле, а позже. СРОК НЕ СОКРАТИЛСЯ ВООБЩЕ. Хендофф #152 считал 2087 × 0.60 ≈ 1250 фактических вызовов при ёмкости 200/сессию = 6-7 сессий. Живая калибровка сейчас показывает actual/budget = 0.71 (n=10, не 0.60), поэтому 1927 × 0.71 ≈ 1368 фактических = ~7 сессий. Уменьшение объёма съедено уточнением коэффициента. Владельцу отвечать «примерно семь сессий», а не «на сессию меньше». СЛАБОЕ МЕСТО ОЦЕНКИ, названо явно: 11 из 23 задач (895 из 1927, то есть 46% суммы) не имеют call_budget вообще — их вклад целиком синтетический, потолок тира по complexity. Это ВЕРХНЯЯ граница по построению: потолок, а не медиана. Проставлять им бюджеты «на глаз» в этой ревизии я не стал — это было бы подменой планирования подгонкой цифры под желаемый срок. Уточнение произойдёт естественно при старте каждой из них.
- 2026-07-28T19:33:40Z [implementation] — ПОБОЧНЫЕ НАХОДКИ РЕВИЗИИ, заведены отдельными карточками — в эту не втягиваются, потому что это код, а ревизия объявлена read-only по коду (scope_exclude). 1. tool-call-syntax-leaks-into-entity-text (90 вызовов). Замечено при чтении tausik/tasks/brain-decide-publishes-unclassified-rationale.md: goal обрывается тегом закрытия поля, следом идёт строка с параметром complexity. Проверил, не единичный ли случай — НЕ единичный: 64 файла, 147 строк по всему дереву tausik/ (задачи 31, память 26, решения 7). В одном случае в scope_exclude уехал целый последующий вызов task_start, в другом — шесть строк чужого вызова с командой Bash. Значение «medium» в теле goal доказывает, что вызов был принят ЧАСТИЧНО и потеря аргументов прошла молча. 2. publish-risk-gate-docstring-lies-after-205 (18 вызовов, defect_of brain-decide-publishes-unclassified-rationale). scripts/brain_publish_flow.py:97 продолжает утверждать «Only patterns/gotchas apply» после того, как решением #205 decisions заведены в _CLASSIFIER_CATEGORY. Эта же строка цитировалась как улика в расследовании #152 — то есть она работает свидетельством о поведении кода и теперь свидетельствует ложно. Обе НЕ входят в объём 1.8 (эпик не назначен) и на пересчёт остатка не влияют.
- 2026-07-28T19:35:24Z [implementation] — ТРЕТЬЯ ПОБОЧНАЯ НАХОДКА, и она блокер релиза: tausik-tree-gitattributes-lf. Обнаружена не рассуждением, а предупреждением git при прогоне `state export` в рамках критерия 4 — «LF will be replaced by CRLF the next time Git touches it» на tausik/stories/kb-git-sync.md. Проверено: .gitattributes закрепляет renar/** на eol=lf и в комментарии называет ровно эту причину, дерево tausik/ такого закрепления не имеет. Измерено на файлах, полученных чекаутом: CHANGELOG.md и README.md — w/crlf; renar/README.md — w/lf. То есть на свежем клоне под Windows все 2095 файлов дерева станут CRLF, а check_tree читает с выключенной universal-newline трансляцией и считает CRLF порчей. Гейт покраснеет гарантированно, на нетронутом дереве, у первого же пользователя. СВЯЗЬ С ЭТОЙ РЕВИЗИЕЙ ПРЯМАЯ. Я только что закрыл kb-export-tasks как поглощённую, назвав её критерий 3 («повторная выгрузка без изменений не порождает диффа») выполненным. Он выполнен — в пределах одной машины. Вердикт остаётся в силе: критерий говорит о детерминизме экспортёра, и экспортёр детерминирован; порчу вносит чекаут git, то есть слой ниже карточки. Но фиксирую явно, чтобы следующий агент не счёл эти два вердикта противоречащими друг другу. ПОБОЧНЫЙ ЭФФЕКТ ЭКСПОРТА, отмечен для критерия 4: `state export --check` показал дрейф tausik/stories/kb-git-sync.md — story перешла open -> active из-за старта задачи, но проекция за этим переходом не последовала. Автотриггер отработал по задачам и решению, по родительской story — нет. Это возможный остаток дефекта state-export-trigger-misses-task-linked-decessions (закрыт в #152 как «проекция следует за ЛЮБОЙ мутацией»). Не завожу карточку из этой ревизии: одного наблюдения мало, каскад мог идти в обход сервисного слоя. Проверю прицельно после закрытия текущих задач и заведу, если воспроизведётся.
- 2026-07-28T19:36:32Z [implementation] — ЧЕТВЁРТАЯ НАХОДКА, воспроизведена и заведена: cascade-mutations-never-project-to-tree. В предыдущей записи я обещал проверить прицельно и завести, если подтвердится — подтвердилось чтением. scripts/service_cascade.py: четыре мутации проецируемых сущностей (строки 24, 28, 38, 43), ноль вызовов проекции, всё напрямую через self.be мимо HierarchyMixin._project. Наблюдавшийся дрейф tausik/stories/kb-git-sync.md — не случайность каскада в обход сервисного слоя, а его нормальное поведение. Заведена как defect_of state-export-trigger-misses-task-linked-decisions: та задача чинила этот же класс и объявляла «проекция следует за любой мутацией», починив шесть мутаторов иерархии и не тронув каскад. ИТОГ ПОБОЧНЫХ НАХОДОК РЕВИЗИИ — ЧЕТЫРЕ, все с воспроизводимым доказательством, ни одна не найдена поиском по подозрению: tool-call-syntax-leaks-into-entity-text (64 файла), publish-risk-gate-docstring-lies-after-205 (ложная улика в коде), tausik-tree-gitattributes-lf (блокер релиза, красный гейт на свежем клоне под Windows), cascade-mutations-never-project-to-tree (эпик закрывается в БД и остаётся активным в git). Ни одна не входит в объём 1.8 по эпику и на пересчёт 1927 не влияет; но tausik-tree-gitattributes-lf обязана быть закрыта ДО тега v1.8.0, потому что ломает заглавную фичу релиза у первого же пользователя. НАБЛЮДЕНИЕ О САМОЙ РЕВИЗИИ, для метода. Все четыре найдены не отдельным поиском дефектов, а КАК ПОБОЧНЫЙ ЭФФЕКТ требования «назови артефакт для каждого критерия». Сверка по формулировкам не нашла бы ни одной: чтобы обнаружить их, надо было открыть файл, запустить экспорт и посмотреть на предупреждение git. Это довод в пользу того, чтобы ревизии объёма всегда требовали артефакт, а не совпадение смысла — они окупаются не только уточнением оценки.
- 2026-07-28T19:39:07Z [implementation] — AC-1: ✓ Не тестом, а сверкой с деревом — критерий по природе про сверку, а не про поведение кода. Запись «СВЕРКА КАРТОЧКИ 1 — kb-export-tasks» выше называет артефакт по каждому из трёх содержательных критериев плюс CHANGELOG: scripts/state_export.py::_task_doc, state_serialize.ENTITY_DIRS, CHANGELOG.ru.md:1681 и восемь поимённых тестов из tests/test_state_export.py. Проверяемость: любой из восьми тестов запускается по имени, любая из строк открывается по номеру. AC-2: ✓ Запись «СВЕРКА КАРТОЧКИ 2 — kb-brain-deprecate», пять критериев, по каждому назван артефакт ИЛИ названо, чего именно не хватает: brain_runtime.py::open_brain_deps:131 (флаг есть), brain_classifier.classify + memory_markers.detect_markers (классификатор на месте), четыре точки проверки приватности поимённо, task list --status planning (kb-global-* не существуют, импортировать некуда). AC-3: ✓ Вердикты вынесены и записаны в журналы САМИХ карточек, а не только сюда: kb-export-tasks — «ПОГЛОЩЕНА ЦЕЛИКОМ эпиком team-state-in-git» с перечнем поглотивших слагов; kb-brain-deprecate — «СУЖЕНА, НЕ закрыта» с указанием, что именно ушло из объёма и что осталось. AC-4: ✓ Применено в БД и доказано git: kb-export-tasks закрыта штатным task done (--no-file-changes, git подтвердил чистоту объявленной области; risk 0.36); kb-brain-deprecate переписана — goal, шесть критериев вместо пяти, call_budget 150->90, добавлена жёсткая зависимость от kb-global-write. Проекция синхронизирована: `tausik state export` записал 2095 файлов, `state export --check` до правки показывал ровно один дрейф, после — чисто; git status показывает изменения обеих карточек. AC-5: ✓ Запись «ПЕРЕСЧЁТ ОСТАТКА ОБЪЁМА 1.8»: 23 открытых, 1927 бюджета. Расхождение с 2087 зафиксировано решением #209, включая то, что срок НЕ сократился (0.60 -> 0.71 съел выигрыш). AC-6 (негатив): ✓ Правило сработало и изменило исход, а не осталось декларацией. kb-brain-deprecate НЕ закрыта именно потому, что артефакт не назван для трёх критериев из пяти — при списании «по совпадению формулировок» она бы закрылась, и оценка остатка занизилась бы ещё на 150. Обратная ветвь тоже проверена: kb-export-tasks закрыта только после того, как артефакт нашёлся для КАЖДОГО критерия, включая дописанный негативный. Domain: осмысленность вне формальной сверки подтверждена побочным результатом. Требование «назови артефакт» заставило открыть файлы и запустить экспорт — и это нашло четыре живых дефекта, включая блокер релиза (tausik-tree-gitattributes-lf: гейт краснеет на свежем клоне под Windows) и эпик, который закрывается в БД и остаётся активным в git (cascade-mutations-never-project-to-tree). Сверка по формулировкам не нашла бы ни одного. Прогон: verification_run #1583, exit=0, no_tests_declared=1, объявленная область 10 файлов — вся, что показывает git status. Гейты SKIP по существу: задача не меняет ни кода, ни тестов, только текст карточек в БД и его проекцию. Закрытие опирается на объявление, а не на выполненный гейт, и это записано в самом прогоне, а не только здесь.
