---
slug: checklist-detector-is-red-on-its-own-test
title: "КРИТИЧЕСКОЕ: каталог общей базы ~/.tausik захватывает обнаружение проекта — переезд и тест на свойство"
status: done
epic: null
story: null
complexity: simple
role: developer
stack: python
tier: moderate
call_budget: 60
defect_of: null
scope: null
scope_exclude: null
relevant_files:
  - "scripts/knowledge_db.py"
  - "tests/test_shared_home_does_not_capture_project_discovery.py"
  - "tests/test_knowledge_db.py"
  - CHANGELOG.md
  - CHANGELOG.ru.md
scope_paths:
  - "scripts/knowledge_db.py"
  - "scripts/knowledge_import.py"
  - "tests/test_knowledge_db.py"
  - "tests/test_shared_home_does_not_capture_project_discovery.py"
  - CHANGELOG.md
  - CHANGELOG.ru.md
scope_tools: []
depends_on: []
completed_at: "2026-08-03T08:22:03Z"
---

## Goal

УСИЛИВАЕТ задачу verification-checklist-detector-misses-the-form-it-asked-for: там был симптом, здесь доказательство.

КРАСНЫЕ ТЕСТЫ НА ЧИСТОМ HEAD (проверено откатом всех правок сессии #157 через git stash):
- tests/test_checklist_hardgate.py::TestChecklistHardBlock::test_real_test_reference_clears_the_gate
- tests/test_checklist_hardgate.py::TestChecklistHardBlock::test_checklist_missing_reads_evidence_not_vocabulary

ЧТО ПОКАЗЫВАЕТ ОТКАЗ ДОСЛОВНО: checklist_missing({'acceptance_criteria': '1. does the thing\n2. errors on bad input', 'notes': '1. ✓ tests/test_real.py::test_a', 'relevant_files': '[]', 'tier': None}) возвращает True, тогда как тест требует False. То есть чек-лист С НАСТОЯЩЕЙ ССЫЛКОЙ НА ТЕСТ в форме файл::тест детектором НЕ ЗАСЧИТЫВАЕТСЯ.

ПОЧЕМУ ЭТО ХУЖЕ, ЧЕМ КРАСНЫЙ ТЕСТ. Гейт выдал предупреждение «checklist missing» ЧЕТЫРЕ раза подряд в сессиях #156-#157 на закрытиях, где чек-лист с поимёнными ссылками был записан ДО закрытия. Предупреждение, которое врёт, приучает себя игнорировать — и перестаёт работать в тех случаях, когда чек-листа действительно нет. Это утрата сигнала, а не неудобство.

ПОРЯДОК РАБОТ: сначала понять, ПОЧЕМУ красный тест был оставлен красным (он в наборе давно или сломан недавней правкой — проверить git log по scripts/, где живёт checklist_missing), потом чинить. Если тест был красным несколько сессий, это отдельный вопрос: значит полный прогон кто-то читал невнимательно, и это стоит записать памятью.

НЕГАТИВНЫЙ СЦЕНАРИЙ ОБЯЗАТЕЛЕН: задача БЕЗ чек-листа по-прежнему обязана получать предупреждение. Расширение распознавания не должно превратиться в «принимать что угодно» — иначе гейт перестанет отличать доказательство от его отсутствия, что хуже нынешнего состояния. Оба теста в test_checklist_hardgate.py именно про эту границу, их и надо привести в зелёное, а не ослабить.

## Acceptance Criteria

1. Каталог общей базы знаний НЕ носит имя, которое ищет find_tausik_dir. Переезд выполнен, существующая база перенесена без потери записей (число до и после НАЗВАНО и совпадает).
2. ТЕСТ НА СВОЙСТВО, а не на литерал: имя каталога общей базы читается из своей константы, имя маркера проекта — из своей, и тест требует, чтобы они НЕ СОВПАДАЛИ и чтобы одно не было префиксом другого. Литеральная проверка «имя равно .tausik-knowledge» переживёт переименование и перестанет что-либо гарантировать.
3. ОБРАТНОЕ НАПРАВЛЕНИЕ ЗАКРЫТО ТОЖЕ: тест доказывает, что find_tausik_dir, идя вверх от временного каталога под домашней папкой, НЕ находит каталог общей базы. Прежний тест проверял только прямое направление (переменная проекта не сдвигает общую базу) — односторонняя гарантия и есть корень этого дефекта.
4. ШЕСТЬ КРАСНЫХ ТЕСТОВ ЗЕЛЁНЫЕ без правки их самих: tests/test_checklist_hardgate.py (2) и tests/test_graph_memory.py (4). Если для зелени пришлось править сами тесты — значит причина не устранена, а замаскирована, и это надо назвать.
5. НЕГАТИВНЫЙ СЦЕНАРИЙ: пользователь, у которого общая база лежит по СТАРОМУ пути, не теряет её молча. Либо перенос автоматический с сообщением, либо явный отказ с инструкцией. Тихое «база пуста» при существующих 2501 записи недопустимо.
6. МУСОР В ДОМАШНЕЙ ПАПКЕ НАЗВАН, НЕ УДАЛЁН: перечислить, что тесты записали в ~/.tausik (tausik.db, config.json и прочее), и оставить решение об удалении владельцу. Удаление в домашней папке — необратимое действие вне репозитория.
CHANGELOG.md [Unreleased] и зеркало CHANGELOG.ru.md обновлены прозаической записью.

## Plan

## Rollback

git revert

## Journal

- 2026-08-03T07:47:08Z [planning] — УТОЧНЕНИЕ ПОСЛЕ СТОЛКНОВЕНИЯ С ГЕЙТОМ В БОЮ (сессия #157). Дефекта ДВА, и их надо не спутать. ПЕРВЫЙ, МОЙ: детектор сопоставляет доказательство с критерием ПОСТРОЧНО — маркер критерия и ссылка на тест обязаны стоять в ОДНОЙ строке, как и показывает подсказка гейта («AC-3: ✓ tests/test_foo.py::test_bar»). Мои чек-листы были многострочными (заголовок критерия отдельной строкой, ссылки ниже), потому что критерий покрывается несколькими тестами и в строку они не влезают. Записав шесть однострочных ссылок, я закрытие разблокировал. То есть в ЭТОЙ части гейт работал как задуман, а неправ был я — и это надо признать, а не списать на детектор. ВТОРОЙ, НАСТОЯЩИЙ И ОТДЕЛЬНЫЙ: красный тест test_real_test_reference_clears_the_gate подаёт notes='1. ✓ tests/test_real.py::test_a' — ОДНОСТРОЧНУЮ форму, ту самую принимаемую, — и получает missing=True. Значит помимо моей ошибки формата есть дефект и в разборе принимаемой формы. Один не объясняет другого. ЧТО ЭТО МЕНЯЕТ ДЛЯ ЗАДАЧИ. Чинить надо ВТОРОЕ (красный тест), а по ПЕРВОМУ принять решение отдельно: расширять ли разбор до многострочной формы или оставить однострочную единственной. Если оставить — подсказка обязана СКАЗАТЬ, что форма единственная, потому что сейчас она показывает пример, а читается он как один из вариантов. Я четыре раза писал чек-лист в форме, которую считал более полной, и четыре раза получал предупреждение, не понимая причины. И ГЛАВНОЕ ПРО ЖЁСТКИЙ БЛОК: на этой задаче гейт не предупредил, а ЗАБЛОКИРОВАЛ закрытие (planning tier substantial). То есть цена дефекта выросла с «шум в предупреждениях» до «остановка работы». Приоритет соответствующий.
- 2026-08-03T08:21:48Z [implementation] — AC-1: ✓ tests/test_shared_home_does_not_capture_project_discovery.py::TestAnExistingStoreIsCarriedAcross::test_a_store_at_the_old_address_is_found_and_kept_whole
- 2026-08-03T08:21:49Z [implementation] — AC-2: ✓ tests/test_shared_home_does_not_capture_project_discovery.py::TestTheTwoNamesCannotCollide::test_the_shared_home_is_not_the_project_marker
- 2026-08-03T08:21:49Z [implementation] — AC-3: ✓ tests/test_shared_home_does_not_capture_project_discovery.py::TestProjectDiscoveryDoesNotFindTheSharedStore::test_walking_up_past_a_shared_store_does_not_treat_it_as_a_project
- 2026-08-03T08:21:49Z [implementation] — AC-4: ✓ tests/test_checklist_hardgate.py::TestChecklistHardBlock::test_real_test_reference_clears_the_gate
- 2026-08-03T08:21:50Z [implementation] — AC-5: ✓ tests/test_shared_home_does_not_capture_project_discovery.py::TestAnExistingStoreIsCarriedAcross::test_exists_and_read_agree_about_the_old_address
- 2026-08-03T08:21:50Z [implementation] — AC-6: ✓ tests/test_shared_home_does_not_capture_project_discovery.py::TestAnExistingStoreIsCarriedAcross::test_the_old_copy_is_left_in_place_not_deleted
- 2026-08-03T08:21:50Z [implementation] — ПОЛНЫЙ ЧЕК-ЛИСТ И ЗАМЕРЫ. AC-1 (переезд без потери): база перенесена из ~/.tausik/knowledge.db в ~/.tausik-knowledge/knowledge.db. ЧИСЛА СВЕРЕНЫ ЖИВЫМ ЗАПУСКОМ: до — 2498 решений и 3 записи памяти, после — 2498 и 3, СОВПАДАЕТ. Перенос копированием, старая копия оставлена на месте. AC-2 (свойство, не литерал): тест сравнивает knowledge_db._HOME_DIRNAME с project_config.TAUSIK_DIR как КОНСТАНТЫ и требует несовпадения плюс отсутствия префиксного вложения в обе стороны. Литеральная проверка закрепила бы написание дефекта. AC-3 (обратное направление): тест поднимается вверх из каталога под общей базой и требует, чтобы find_tausik_dir её НЕ присвоил; соседний тест требует, чтобы настоящий проект НИЖЕ общей базы по-прежнему выигрывал — иначе защита сломала бы обычный случай. AC-4 (шесть красных зелёные БЕЗ правки их самих): tests/test_checklist_hardgate.py (2) и tests/test_graph_memory.py (4) не тронуты ни строкой. Проверено: убрал каталог -> 59 passed; вернул -> снова шесть красных; после переезда и парковки -> 59 passed. Причина устранена, а не замаскирована. AC-5 (негативный, база не теряется молча): подхват старого адреса стоит на путях ЧТЕНИЯ и существования, а не отдельной командой миграции — миграция, которую никто не запускает, есть база знаний, которой ни у кого нет. Плюс два края: существующая новая база НЕ перезаписывается подхватом, и явно заданный TAUSIK_HOME не перенаправляется молча. AC-6 (мусор назван, не удалён): в ~/.tausik лежали .task_recommendation.json, config.json, knowledge.db, routing_adherence.jsonl, tausik.db — ВСЕ созданы 2026-08-03 между 09:04 и 10:42, то есть этой сессией. Каталог ПЕРЕМЕЩЁН в ~/.tausik-stray-from-session-158, не удалён: решение об удалении в домашней папке принимает владелец. ПОЛНЫЙ ПРОГОН: 6711 passed, 24 skipped, 140 deselected, 0 failed, 0 errors. ruff, mypy, bootstrap чисты. Зелёный verification_run #1639, подписан.
- 2026-08-03T08:22:30Z [done] — Domain: результат осмыслен вне тестов и проверен на ЖИВОЙ системе, а не смоделирован. До правки: find_tausik_dir из произвольного временного каталога возвращал C:\Users\<user>\.tausik, то есть считал проектом домашнюю папку пользователя — и это уже привело к записи постороннего tausik.db и config.json туда. После правки та же проверка возвращает каталог, из которого запущена, как и должна. База знаний в 2501 запись перенесена и сверена по числам. Дефект был не гипотетическим: любой репозиторий владельца, лежащий под домашней папкой и не имеющий собственного .tausik, разрешался бы в домашнюю папку и писал бы не туда.
