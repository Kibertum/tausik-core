---
slug: check-result-conflates-could-not-run-with-passed
title: "Результат проверки двузначен: «не смогло выполниться» неотличимо от «прошло»"
status: planning
epic: release-19-renar-conformance
story: evidence-primitives
complexity: null
role: architect
stack: python
tier: substantial
call_budget: 150
defect_of: null
scope: null
scope_exclude: null
relevant_files: []
scope_paths: []
scope_tools: []
depends_on: []
completed_at: null
---

## Goal

ПЕРВАЯ ЗАДАЧА 1.9 — на ней стоит всё остальное. У нас нет типа результата проверки: есть строки, сентинелы и соглашения. _SCOPED_SKIP_SENTINEL, _NO_IMPL_SENTINEL, SKIP от отсутствия отображения, SKIP от отсутствия инструмента, тихий возврат к умолчанию при отвергнутой команде гейта — всё это разные события, читающиеся как одно, и ни одно не краснеет.

СОБСТВЕННЫЙ ПРОТОКОЛ, а не гипотеза: гейт pytest вырождался в no-op вне корня тестов, и SKIP был неотличим от честного (тикет #8, внешний репортёр); гейт memory-route у потребителя не находил себя и МОЛЧА не запускался; детектор соседних MCP отдавал ноль, означавший «не умею считать»; отвергнутая команда гейта не краснела, а откатывалась к умолчанию (память #: gotcha 1.8). Один корень: результат имеет два значения там, где событий три.

ЧТО ДЕЛАЕТСЯ: результат проверки становится ТИПОМ с тремя исходами — выполнилось и прошло, выполнилось и не прошло, НЕ СМОГЛО ВЫПОЛНИТЬСЯ. Третий исход несёт ПРИЧИНУ и по умолчанию блокирует, а не пропускает. Это ровно SENAR 1.4 §8.6(e): отсутствие отрицательной находки не есть положительный вердикт. Квитанция перестаёт подписывать пустоту.

НЕГАТИВНОЕ ОГРАНИЧЕНИЕ: «законный пропуск» обязан остаться выразимым — изменение, которому честно не соответствует ни один тест, есть нормальный случай и не блокирует. Различие между ним и невозможностью выполниться и есть весь предмет задачи; свести оба к блокировке значит заменить одну неразличимость другой.

## Acceptance Criteria

## Plan

## Rollback

## Journal

- 2026-08-25T19:15:57Z [planning] — ЖИВОЙ ПРИЁМОЧНЫЙ ПРИМЕР, НАЙДЕННЫЙ СЛУЧАЙНО В СЕССИИ #182 — зеркальная половина этого же дефекта, воспроизводится одной командой. .tausik/tausik verify --task skill-cli-negative-tests-never-reach-the-skill-layer --relevant-files tests/test_skill_cli_help.py [FAIL] pytest (block) SCOPE: scoped run over 1 of 403 test file(s) ... tests/test_skill_cli_help.py 5 deselected in 0.14s Recorded verification_run #1807 (exit=1). НИ ОДИН ТЕСТ НЕ УПАЛ. Ни один и не запустился: файл помечен pytest.mark.slow, скоупный прогон наследует addopts = -m 'not slow', все пять тестов деселектированы. Гейт объявил [FAIL] (block) там, где верный ответ — «прогон не состоялся». Это ровно предмет задачи, только с другой стороны: заголовок говорит про «не смог запуститься, а записано passed», здесь — «не смог запуститься, а записано failed». Обе половины следуют из одного и того же: результат проверки не отличает «выполнено и вот вердикт» от «не выполнялось». ВТОРАЯ ПОЛОВИНА ХУЖЕ ПЕРВОЙ ПО ПОСЛЕДСТВИЯМ ДЛЯ АГЕНТА. Отказ не называет ПРИЧИНУ и не называет средство. Строка «5 deselected» в выводе есть, но вердикт [FAIL] уводит читателя чинить тесты, которых никто не запускал. Верное средство — TAUSIK_VERIFY_FULL=1 — не упомянуто ни словом; с ним та же команда даёт [PASS] и выдаёт handle (verification_run #1808, exit=0). Расстояние между «блокирующий провал» и «всё зелёно» здесь равно одной переменной окружения, о которой отказ молчит. СЛЕДСТВИЕ ДЛЯ КОНТРАКТА: задача, чей фикс целиком лежит в slow-помеченном тестовом файле, НЕ МОЖЕТ БЫТЬ СЕРТИФИЦИРОВАНА штатным verify --task. Это не редкий угол: negative/e2e/subprocess-тесты помечены slow массово. ПОПУТНО, ОТДЕЛЬНЫЙ МЕЛКИЙ ДЕФЕКТ ДОКУМЕНТАЦИИ. pyproject.toml:34-35 отсылает к «tausik verify --full (env: TAUSIK_VERIFY_FULL=1)». Флага --full у команды НЕТ: verify --help показывает только --task, --scope, --relevant-files, --no-tests-expected. Работает переменная окружения; названный рядом с ней флаг — обещание, которого продукт не выполняет.
