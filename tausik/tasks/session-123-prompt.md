---
slug: session-123-prompt
title: "Обновить prompt.md под состояние после сессий #117-#122 (отстал на восемь сессий)"
status: done
epic: landscape-2026-h2
story: l26-arch-debt
complexity: simple
role: tech-writer
stack: null
tier: light
call_budget: 25
defect_of: null
scope: null
scope_exclude: null
relevant_files:
  - prompt.md
scope_paths:
  - prompt.md
scope_tools: []
depends_on: []
completed_at: "2026-07-20T13:50:46Z"
---

## Goal

prompt.md — ВХОДНАЯ ТОЧКА новой сессии: владелец даёт его агенту первым, до /start. Сейчас он от 2026-07-18, описывает состояние сессии #114 и отстал на восемь сессий.

ПОЧЕМУ ЭТО НЕ КОСМЕТИКА. Устаревший входной документ хуже отсутствующего: агент читает его ДО того, как получит факты, и строит на нём план. Ошибки там распространяются на всю сессию.

ЧТО В НЁМ СЕЙЧАС НЕВЕРНО (проверено):
1. «kb-uid-model обязателен ДО первой выгрузки знаний, kb-global-schema без неё стартовать не должен» — такого слага В БАЗЕ НЕТ, роль исполняет km-stable-identity-backfill (поправка решением #154, сессия #122).
2. Объём релиза 1.8 назван как 20+15=35 задач по решению #141. Факт сегодня: landscape-2026-h2 = 30 открытых, shared-knowledge = 20, итого 50. Расхождение известно с сессии #117 (память #233 зафиксировала 41) и не чинилось пять сессий.
3. «Локальный main опережает origin на 11 коммитов» — сегодня 21.
4. «Рабочее дерево чистое, про коммит спрашивать не нужно» — было верно тогда; после четырёх сессий без коммита стало неверно и стоило блокировки QG-2 в #121.
5. Раздел «Куда идти дальше» перечисляет задачи, часть которых уже закрыта (l26-gate-command-neutering, audit-stale-docs-scope-noise).

ОТДЕЛЬНАЯ НАХОДКА, КОТОРУЮ НАДО ЗАФИКСИРОВАТЬ, А НЕ ПРОСТО ПЕРЕПИСАТЬ ЦИФРУ. Перепланирование границы релиза 1.8 в сессии #120 НЕ ЗАПИСАНО РЕШЕНИЕМ: handoff #120 ссылается на «решение #153», но #153 в базе — это премиса эпика v2-global-mcp, записанная в #121. То есть номер был назван до записи и не совпал. Авторитетно объём релиза до сих пор определяется решением #141, устаревшим трижды. Новый prompt.md обязан назвать это открытым вопросом к владельцу, а не тихо подставить свежее число: цифра без решения — это ещё одна копия, которая разъедется.

ГРАНИЦА. Файл в .gitignore (строка 79) и вне git — ни один аудит до него не дотягивается, поэтому его актуальность держится только дисциплиной. Это довод завести проверку, но НЕ в этой задаче: здесь только обновление текста.

## Acceptance Criteria

AC1. prompt.md описывает состояние ПОСЛЕ сессии #122: версия, число коммитов впереди origin, чистота дерева, номер последней сессии, счётчик задач — все числа проверены командой, а не перенесены из прежней версии.
AC2. Пять названных в goal неверных утверждений исправлены поимённо; ни одно не осталось.
AC3. Открытый вопрос об объёме релиза 1.8 НАЗВАН как открытый (решение #141 устарело, перепланирование #120 решением не записано), а не закрыт подстановкой свежего числа. Свежее число приводится как ФАКТ ЗАМЕРА с датой, отдельно от того, что решено.
AC4. Раздел «куда идти дальше» отражает реальную очередь: первой ddl-parity-marker-leak-and-column-miscount (гейт молча прячет дрейф), затем stack-gates-orphaned-flutter-swift (запрос владельца, релиз 1.8).
AC5. НЕГАТИВНЫЙ: закрытые задачи в очереди не упоминаются. Проверяется тем, что каждый названный слаг имеет статус planning или blocked на момент записи; задача со статусом done в файле — ошибка.
AC6. НЕГАТИВНЫЙ: ни одно утверждение о числах не переносится из прежней версии без перепроверки. Отсутствие проверки приравнивается к ошибке, потому что именно так прежний файл и протух.
AC7. Гочи сессий #121-#122 включены: ложная уверенность собственного негативного теста, разбор применённый наполовину, bootstrap drift, порядок relevant_files -> verify -> done, незакоммиченный долг блокирует QG-2.

## Plan

## Rollback

## Journal

- 2026-07-20T13:50:28Z [implementation] — AC-1: ✓ Файл описывает состояние после #122, все числа проверены КОМАНДОЙ в момент записи и перепроверены контрольным прогоном: коммитов впереди origin 21 (git rev-list --count), дерево чистое (git status --porcelain = 0 строк), последняя сессия #122, задачи 995/1070 done. Числа, способные двигаться, помечены как живые со ссылкой на /start. AC-2: ✓ Все пять неверных утверждений исправлены поимённо. Контрольный grep по prompt.md показывает, что оставшиеся вхождения «kb-uid-model», «11 коммитов», «20+15 = 35», «спрашивать не нужно» — это ТЕКСТЫ САМИХ ИСПРАВЛЕНИЙ («прежняя версия утверждала X, это неверно»), а не сохранившиеся утверждения. Раздел «Куда идти дальше» переписан заново: закрытые l26-gate-command-neutering и audit-stale-docs-scope-noise убраны. AC-3: ✓ Объём релиза 1.8 назван ОТКРЫТЫМ ВОПРОСОМ, а не закрыт подстановкой. В файле разведены четыре разные величины с источниками: решение #141 = 35 (авторитетно, устарело), память #233 = 41, handoff #120 = 46 со ссылкой на несуществующее решение, замер 2026-07-20 = 51. Отдельно зафиксировано главное: перепланирование границы решением НЕ ЗАПИСАНО — handoff #120 сослался на «#153», а #153 в базе оказалось премисой эпика v2-global-mcp, записанной позже в #121 (проверено decisions_list). Дана команда для самостоятельного перемера. AC-4: ✓ Очередь отражает реальность: первой ddl-parity-marker-leak-and-column-miscount с объяснением, почему именно она (гейт молча прячет дрейф, то есть делает обратное назначению), второй stack-gates-orphaned-flutter-swift с готовой разведкой. AC-5: ✓ НЕГАТИВНЫЙ выполнен МЕХАНИЧЕСКИ, а не просмотром: извлёк все слаги из файла регуляркой и прогнал `task show` по каждому — задач со статусом done среди названных НЕТ (вывод пуст). Предварительно статусы одиннадцати ключевых слагов проверены поимённо. AC-6: ✓ НЕГАТИВНЫЙ сработал НА МНЕ и поймал реальную ошибку: контрольная сверка показала landscape-2026-h2 = 31 против записанных 30. Причина — я сам завёл session-123-prompt в этот эпик между замером и записью. Исправлено на 31, и сам случай внесён в файл как иллюстрация того, что число живое. Второе такое же расхождение поймано на счётчике задач (1069 -> 1070) и тоже исправлено. Без этого критерия файл ушёл бы с двумя свежими неточностями — ровно тем способом, которым протухла прежняя версия. AC-7: ✓ Гочи #121-#122 включены все названные: ложная уверенность собственного негативного теста (#257), разбор применённый наполовину, bootstrap drift (#256), жёсткий порядок relevant_files -> verify -> done, незакоммиченный долг блокирует QG-2 (#254). Плюс разбор храповика (#252), свежая схема ≠ мигрированная (#253), выборочная правка постановок, tausik decide с task_slug, проверка агентов пишущих файлы. Negative: закрытые задачи в очередь не попали (проверено машинно по всем слагам файла); числа не перенесены из прежней версии — два из них перепроверка поймала как уже устаревшие и они исправлены; открытый вопрос об объёме релиза НЕ закрыт подстановкой свежей цифры, потому что цифра без решения — ещё одна копия, которая разъедется (конвенция #249). Domain: prompt.md — входная точка сессии, владелец даёт его агенту ДО /start. Ошибка здесь распространяется на всю сессию, потому что читается раньше фактов. Файл в .gitignore (строка 79), поэтому ни один аудит до него не дотягивается и его актуальность держится только дисциплиной — это названо в карточке как довод завести проверку отдельной задачей, а не сделано здесь. Checklist: работа велась через задачу, как и предшественница session-114-prompt — обновление входного документа здесь ведётся задачей, а не походя; прежний файл прочитан ЦЕЛИКОМ до перезаписи; ни одно утверждение не перенесено без проверки; найденная по дороге дыра в учёте (перепланирование границы релиза без решения) не замолчана и не «починена» подстановкой, а вынесена владельцу.
