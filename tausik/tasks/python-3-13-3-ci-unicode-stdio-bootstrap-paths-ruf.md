---
slug: python-3-13-3-ci-unicode-stdio-bootstrap-paths-ruf
title: "Python 3.13 совместимость: 3 теста красят CI (unicode_stdio + bootstrap_paths), пряталось за ruff"
status: done
epic: null
story: null
complexity: null
role: developer
stack: python
tier: null
call_budget: null
defect_of: null
scope: null
scope_exclude: null
relevant_files:
  - "tests/test_bootstrap_paths.py"
  - "tests/test_unicode_stdio.py"
scope_paths: []
scope_tools: []
depends_on: []
completed_at: "2026-07-17T17:52:30Z"
---

## Goal

CI v1.7.0 красный ТОЛЬКО на Python 3.13 (3.11/3.12 зелёные). Три теста: test_unicode_stdio::test_cp1251_stdout_crashes_without_fix (UnicodeDecodeError при чтении stderr субпроцесса), test_bootstrap_paths::test_in_project_becomes_var_relative и ::test_project_root_itself_maps_to_var (AssertionError на путях). Предсуществующее — пряталось за красным ruff (ruff падал раньше pytest, поэтому pytest на 3.13 НИКОГДА не запускался в CI; v1.7.0 CI не был по-настоящему зелёным на трети матрицы). НЕ мой регресс. Автор гоняет локально 3.11, поэтому не видел — но на машине ЕСТЬ py -3.13 (3.13.14), фиксим не вслепую.

## Acceptance Criteria

1. Все три падения ВОСПРОИЗВЕДЕНЫ локально через py -3.13, первопричина каждого названа (поведение 3.13: кодировка stdio / os.path), не угадана. 2. Каждый тест/код исправлен по первопричине: либо тест обновлён под корректное поведение 3.13 (если менялось поведение платформы, а не наш код), либо наш код починен (если это реальная несовместимость). Различить эти два случая явно. 3. НЕГАТИВНЫЙ СЦЕНАРИЙ: тесты ПРОХОДЯТ на 3.13 И на 3.11 — py -3.13 -m pytest <files> зелёный И python -m pytest <files> зелёный; ОШИБКА, если фикс чинит 3.13, но ломает 3.11 (или наоборот). 4. Полный прогон на 3.13 локально: py -3.13 -m pytest tests/ -q зелёный (обе полосы). 5. ruff/mypy-цепочка не сломана. 6. После пуша — дождаться ЗЕЛЁНОГО CI на всей матрице (3.11/3.12/3.13 × 3 ОС), не выдавать локальный прогон за CI. Тег v1.7.0 передвинуть на зелёный коммит по дуальной схеме (решение #135/#125).

## Plan

## Rollback

## Journal

- 2026-07-17T17:52:30Z [implementation] — AC verified. CI ЗЕЛЁНЫЙ НА ВСЕЙ МАТРИЦЕ (run 29599616986, commit bf13a09, FINAL: success): все 9 ячеек 3.11/3.12/3.13 × ubuntu/windows/macos прошли, включая три 3.13-ячейки, что и было целью. Впервые за релиз v1.7.0 CI по-настоящему зелёный. 1. ✓ Все три падения ВОСПРОИЗВЕДЕНЫ локально через py -3.13 (3.13.14 на машине), первопричина каждого названа: (a) test_bootstrap_paths x2 — Python 3.13 сменил ntpath.isabs (drive-relative '\\work\\proj' без буквы диска теперь non-abs; подтверждено прямым сравнением 3.11=True vs 3.13=False), из-за чего guard portable_path на не-абсолютный путь стал срабатывать; (b) test_unicode_stdio — на UTF-8 локали (Linux) + 3.13 harness строго декодировал намеренно-битый cp1251-вывод дочернего процесса → UnicodeDecodeError из subprocess.run. 2. ✓ Различил тест-vs-код ЯВНО: оба — правки ТЕСТОВ, не продукта. portable_path корректен (реальный project_dir всегда полностью абсолютный) — фикстура строила нереалистичный путь, заякорил os.path.abspath. _run строго декодировал заведомо-битый вывод — errors="replace" держит harness живым, ASCII-фраза 'UnicodeEncodeError' в ассерте выживает. Продукт не тронут. 3. ✓ НЕГАТИВНЫЙ СЦЕНАРИЙ: все затронутые тесты зелёные на py -3.13 И python 3.11 (по 9/9 и 18/18 локально); фикс не ломает ни одну версию. 4. ✓ Полный CI-эквивалент на локальном py -3.13 прогнан ДО пуша (fast lane, те же ignore что в CI). Два всплывших «падения» доказаны как артефакты МОЕГО окружения, не CI: ble001 = 'No module named ruff' (в py -3.13 не стоит ruff; CI ставит), gen_doc_constants = расхождение сбора в локальном Windows Store py -3.13 — прошлый CI-прогон ДОШЁЛ до pytest на 3.13, значит doc-check (шаг раньше) там прошёл, т.е. чистый CI-3.13 собирает те же 4737. Подтверждено финальной зелёной матрицей. 5. ✓ ruff/mypy-цепочка не сломана (ruff All checks passed на 3.11; mypy мягкий). 6. ✓ После пуша ДОЖДАЛСЯ зелёного CI на всей матрице — не выдал локальный прогон за CI. Тег v1.7.0 передвинут на зелёный коммит по дуальной схеме (GitLab 2ec833a, зеркало bf13a09, деревья идентичны, решение #135/#125). СВЯЗЬ С ИССЛЕДОВАНИЕМ: этот инцидент — живое подтверждение находки роя №1 (CI гоняет только fast lane / зелёное табло над красным main). Красный ruff (ранний жёсткий шаг) ПРЯТАЛ предсуществующую 3.13-несовместимость: pytest на 3.13 в CI не запускался никогда, поэтому v1.7.0 CI не был по-настоящему зелёным на трети матрицы, а выглядел зелёным. Класс «одна ошибка прячет другую». Domain: вне тестов — badge релиза теперь честно зелёный на всех заявленных Python (3.11/3.12/3.13) и трёх ОС; продукт не менялся, правки только в двух тест-файлах.
