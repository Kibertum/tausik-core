---
slug: service-knowledge-one-line-from-filesize-gate
title: "service_knowledge.py на 499 строках при жёстком лимите 500: следующая правка в этом файле упрётся в блокирующий гейт"
status: done
epic: null
story: null
complexity: simple
role: developer
stack: python
tier: moderate
call_budget: 35
defect_of: null
scope: "scripts/service_knowledge.py (вынос блока decide/_decision_local/_local_reason/_is_working_project_db), новый scripts/service_knowledge_decide.py, точки импорта. После правки scripts/ обязателен bootstrap --ide all."
scope_exclude: "НЕ менять поведение decide, классификатор, гейт риска и гард принадлежности БД — правка регистра путей заведена отдельно (brain-db-binding-ignores-path-case) и идёт ПОСЛЕ этого разреза. НЕ добавлять exemption в filesize-гейт вместо разреза: это заглушило бы гейт, а не ответило ему. НЕ трогать остальные service_knowledge_*.py."
relevant_files:
  - "scripts/service_knowledge.py"
  - "scripts/service_decide.py"
scope_paths: []
scope_tools: []
depends_on: []
completed_at: "2026-08-01T11:10:50Z"
---

## Goal

Найдено ревью сессии #153. Профилактика, а не дефект — но с датой срабатывания «следующее изменение».

scripts/service_knowledge.py вырос с 394 до 499 строк в партии #152 (+117/-12). Жёсткий лимит filesize-гейта — 500 (CLAUDE.md, промежуточный лимит по решению #190). Гейт сейчас зелёный, запас — одна строка. При этом файл лежит ровно на пути ближайших задач: brain-db-binding-ignores-path-case правит _is_working_project_db, kb-brain-deprecate будет снимать классификатор с decide. Обе упрутся в гейт и получат внеплановый рефакторинг посреди фикса — худший момент для разреза.

ГОТОВЫЙ РАЗРЕЗ, назван ревью. Блок decide/_decision_local/_local_reason/_is_working_project_db (строки 164-289, около 126 строк) — самостоятельная единица. У файла уже есть четыре прецедента такого выноса: service_knowledge_hygiene.py, service_knowledge_aggregates.py, service_knowledge_exploration.py, service_cq_row.py, то есть конвенция разреза в проекте установлена и подражать нечему новому.

ПРОВЕРИТЬ ПРИ ИСПОЛНЕНИИ, конвенции проекта на этот счёт жёсткие: разрез проверяется ЗАПУСКОМ артефакта, а не импортом (память #346 — точка входа теряется незаметно), и строка документации о новом модуле обязана говорить, ЧТО он гарантирует, а не перечислять сущности (память #348).

Остальные файлы партии с запасом: service_task.py 435, service_task_done.py 411, state_import.py 460, state_export.py 393 — не трогать.

## Acceptance Criteria

1. scripts/service_knowledge.py уходит из зоны срабатывания filesize-гейта с запасом, а не на одну строку: после разреза он не длиннее 400 строк.
2. Вынесенный модуль самостоятелен и назван по гарантии, а не по перечню: его докстринг говорит, ЧТО модуль обеспечивает, а не какие сущности содержит (память #348).
3. Разрез проверен ЗАПУСКОМ артефакта, а не импортом (память #346): прогнаны реальные CLI-команда и MCP-инструмент, использующие decide, и обе отработали. Факт запуска и вывод записаны в журнал — точка входа теряется незаметно именно при проверке импортом.
4. НЕГАТИВ И ПОВЕДЕНИЕ. (а) Поведение не изменилось: все существующие тесты decide (tests/test_decide_classifies_what_it_publishes.py и tests/test_service_knowledge_decide.py, 10 + 21) зелёные БЕЗ правки ожиданий; правка ожидания в этих файлах считается нарушением критерия. (б) Публичная поверхность класса не выросла — class_surface-гейт зелёный. (в) Обратной совместимости импортов достаточно: то, что импортировалось из service_knowledge, продолжает импортироваться оттуда же, либо все точки импорта перечислены и обновлены.
5. Полный pytest зелёный; ruff и mypy чистые; filesize-гейт зелёный; bootstrap drift отсутствует.
Записи в CHANGELOG НЕ ДОБАВЛЯЮТСЯ: чистый рефакторинг без пользовательского изменения. Закрытие с --no-changelog, причина в журнале.

## Plan

## Rollback

git revert. Чистое перемещение кода без изменения поведения; схема и миграции не затрагиваются. Откат безопасен и возвращает файл к 499 строкам, то есть к состоянию «одна строка до блокирующего гейта».

## Journal

- 2026-08-01T10:59:23Z [implementation] — AC-3 ВЫПОЛНЕН ЗАПУСКОМ АРТЕФАКТА, не импортом (память #346). Оба входа отработали ПОСЛЕ разреза и ПОСЛЕ bootstrap --ide all (важно: CLI-обёртка грузит .claude/scripts, то есть развёрнутую копию, — импорт из scripts/ тут ничего бы не доказал). CLI: `.tausik/tausik decide "..." --rationale "..."` -> «Decision #213 recorded — saved to local and mirrored to brain ... Page: 3af6b6ed-07ff-8148-8a63-ee88987d2758». Прошла ветка классификации и публикации, то есть самая длинная. MCP: `tausik_decide` с task_slug -> «Decision #214 recorded — saved to local (reason: linked to task service-knowledge-one-line-from-filesize-gate)». Прошла ветка раннего возврата. Вместе две ветки покрывают обе гарантии модуля. СЕДЬМАЯ АВТОСТРАНИЦА NOTION, И ЭТО МОЯ ОПЛОШНОСТЬ ВВОДА, А НЕ НОВЫЙ ДЕФЕКТ. Решение #213 ушло в brain с причиной «no project-specific markers detected», хотя его rationale целиком про brain_move, про исключение (decisions,DELETE) в храповике и про слаг задачи этого репозитория. Классификатор снова не увидел маркеров там, где они есть, — это ровно дефект brain-decide-publishes-unclassified-rationale, теперь семь страниц. Оплошность в том, что CLI-вызов сделан БЕЗ --task: с привязкой он ушёл бы в ранний локальный возврат, как #214. Удаление страницы — решение владельца, самостоятельно не действую.
- 2026-08-01T10:59:55Z [implementation] — AC-ДОКАЗАТЕЛЬСТВА (записаны ДО verify). AC-1 ✓ scripts/service_knowledge.py: 499 -> 377 строк. Запас до жёсткого лимита 500 — 123 строки, до объявленного в критерии порога 400 — 23. Вынесено 127 строк, добавлен делегат из 5. AC-2 ✓ scripts/service_decide.py назван по ГАРАНТИИ: докстринг открывается фразой «A recorded decision always reaches the project; it reaches the outside only on proof» и разбирает ДВА инварианта (безусловная локальная копия через единственную точку write_local; внешняя публикация только при доказанной привязке, fail-closed), а не перечисляет, какие функции внутри. Решение #214. AC-3 ✓ отдельная запись журнала выше: CLI (#213, ветка публикации) и MCP (#214, ветка раннего возврата), обе ПОСЛЕ bootstrap. AC-4 ✓ (а) tests/test_service_knowledge_decide.py (21) + tests/test_decide_classifies_what_it_publishes.py (10) = 31 passed БЕЗ ЕДИНОЙ ПРАВКИ ожиданий; это работает потому, что тесты патчат исходные модули (project_config, brain_config, brain_classifier), а не внутренности service_knowledge, а импорты в record() остались локальными — то есть точка патча не зависит от того, в каком модуле живёт функция. (б) class_surface зелёный, 94 класса: новый модуль состоит из ФУНКЦИЙ, класс не добавлен, а мисксин ПОТЕРЯЛ три метода (_local_reason, _is_working_project_db, _decision_local) — поверхность сузилась, а не выросла. (в) точек импорта обновлять нечего: grep по scripts/ и tests/ показал НОЛЬ внешних обращений к трём вынесенным методам; всё, что импортировалось из service_knowledge (CQ_SOURCE, build_cq_row), импортируется оттуда же. AC-5 — прогон полного pytest идёт, ruff All checks passed, ruff format чисто, mypy Success 294 files (было 293: +service_decide.py), filesize гейт зелёный, bootstrap --ide all прогнан до запусков AC-3. Причина закрытия с --no-changelog: чистый перенос кода без изменения поведения. Пользователь не видит ничего нового — те же команды, те же сообщения, те же 31 тест без правки ожиданий. Запись в CHANGELOG описывала бы внутреннюю границу файлов, что для читателя релиза шум.
- 2026-08-01T11:10:48Z [implementation] — AC-доказательства полностью в журнале. AC1 499->377 строк, запас 123 до жёсткого лимита; AC2 модуль назван по гарантии, докстринг излагает два инварианта, решение #214; AC3 запуск артефакта после bootstrap — CLI (#213, ветка публикации) и MCP (#214, ветка раннего возврата); AC4 31 тест decide зелёные без правки ожиданий, class_surface зелёный и поверхность сузилась на три метода, внешних точек импорта ноль (grep); AC5 полный pytest 6534 passed / 0 failed, ruff чисто, ruff format чисто, mypy Success 294 files, filesize зелёный, bootstrap drift отсутствует. Negative: обе стороны гарантии проверены существующими тестами без правки — test_foreign_db_never_publishes (чужая БД не публикует) и test_unknown_db_provenance_fails_closed (неразрешимая принадлежность fail-closed) остались зелёными, то есть перенос не ослабил fail-closed. Domain: обе гарантии наблюдаемы вне тестов — реальный CLI-вызов вернул «mirrored to brain» с настоящим page id, реальный MCP-вызов вернул локальную запись с причиной привязки к задаче.
- 2026-08-01T11:11:01Z [done] — Закрытие предупредило: «5 AC criteria, but only 4 have explicit evidence markers». Причина — в поле evidence при закрытии маркеры ✓ не проставлены построчно, хотя в журнале они есть. Проставляю явно, чтобы запись читалась без сверки с журналом: AC-1 ✓ 499->377 строк. AC-2 ✓ модуль назван по гарантии, докстринг излагает два инварианта. AC-3 ✓ CLI #213 и MCP #214 после bootstrap. AC-4 ✓ (а) 31 тест без правки ожиданий, (б) class_surface зелёный и поверхность сузилась, (в) внешних точек импорта ноль. AC-5 ✓ полный pytest 6534 passed / 0 failed, ruff, ruff format, mypy 294 files, filesize, bootstrap drift — всё чисто.
