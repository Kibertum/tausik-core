---
slug: r18-docs-falsified-by-breaking-changes
title: "Документация утверждает поведение, которое 1.8 сломала: Notion-заголовок общей базы, классификатор в списке enforcement, обещание зеркалирования decide"
status: done
epic: landscape-2026-h2
story: l26-narrative
complexity: medium
role: tech-writer
stack: null
tier: moderate
call_budget: 45
defect_of: null
scope: "Только три проверенных ложных утверждения + их зеркала ru/en. НЕ роевое переписывание доков — оно вынесено в 1.9 решением #225."
scope_exclude: "[\"scripts/\",\"harness/\",\"tests/\"]"
relevant_files:
  - "docs/en/mcp.md"
  - "docs/ru/mcp.md"
  - "docs/en/shared-brain.md"
  - "docs/ru/shared-brain.md"
  - "docs/en/sessions.md"
  - "scripts/docs_lint.py"
  - "scripts/doc_drift_scanners.py"
  - "scripts/audit_stale_docs.py"
scope_paths:
  - "docs/en/*"
  - "docs/ru/*"
scope_tools: []
depends_on: []
completed_at: "2026-08-03T13:46:31Z"
---

## Goal

Решение #225. Три проверенных места, где доки описывают снятое поведение как текущее: docs/en/shared-brain.md:1 зовёт общую базу Notion-backed (снято #222); там же Enforcement п.2 называет классификатор решающим local-vs-brain (снят #221); docs/{en,ru}/mcp.md:279-284 обещают зеркалирование tausik_decide в Notion (снято #221). Все doc-гейты зелёные — они считают только известные сущности, поэтому их молчание не свидетельство. Тег без этой починки выпускает неверное руководство.

## Acceptance Criteria

AC1: docs/en/shared-brain.md и его ru-зеркало больше не называют общую базу Notion-backed; описан локальный каталог по #222.
AC2: список Enforcement больше не называет классификатор решающим local-vs-brain; описано, что видимость определяет автор (#221).
AC3: docs/{en,ru}/mcp.md не обещают зеркалирование tausik_decide в Notion; сказано, что decide НИКОГДА не публикует автоматически.
AC4: ru и en описывают ОДИН набор утверждений — расхождение зеркал проверено построчно.
AC5 (негативный): если найдено ещё одно ложное утверждение того же класса, оно НЕ чинится молча сверх объёма и НЕ игнорируется — заводится строкой в журнал задачи с указанием файла и строки; пустой список найденного при непроверенных файлах считается провалом AC, а не успехом.

## Plan

## Rollback

Правки только в docs/; git revert одного коммита. Кода не касается.

## Journal

- 2026-08-03T13:45:02Z [implementation] — НАЙДЕНО СВЕРХ ОБЪЯВЛЕННОГО (AC5) — фиксирую, а не чиню молча и не игнорирую. ПОПРАВКА К СОБСТВЕННОЙ ПОСТАНОВКЕ. AC1 был сформулирован неточно, и я это не заминаю. Я писал, что заголовок docs/en/shared-brain.md:1 'cross-project knowledge on Notion' опровергнут решением #222. Это НЕ так: #222 переселил ОБЩЕЕ ЛОКАЛЬНОЕ ХРАНИЛИЩЕ (~/.tausik-knowledge/knowledge.db), а shared-brain.md описывает Notion-brain — другую сущность, и для неё заголовок верен. Настоящий дефект другой и хуже: с 1.8 кросс-проектных хранилищ стало ДВА, документ описывает только одно и не предупреждает о смешении. Починено врезкой 'три хранилища' с таблицей назначений, а не переписыванием заголовка. ДОПОЛНИТЕЛЬНО НАЙДЕНО, того же класса: 1. docs/{en,ru}/mcp.md — абзац про brain move --to-brain обещал 'перенести решения, сохранившиеся локально во время мисконфига'. Ложь той же природы: решения теперь локальны ПО ПРАВИЛУ, очередь на выгрузку не копится. Переписано. 2. docs/en/whats-new-1.8.md — англоязычная страница заметок к тегу отправляла читателя в РУССКИЕ доки пятью ссылками (строки 118, 134, 139, 141, 142), причём docs/en/upgrade.md и docs/en/receipts.md существуют. Это источник заметок к тегу по пункту 5 плана — исправлено. 3. docs/en/sessions.md ОТСУТСТВОВАЛ, при том что docs/ru/sessions.md заведён в 1.8. Создан полный EN-зеркальный документ (90 строк), ссылка переведена на него. Оставлена одна честная ссылка в ru: agent-contract.md существует только по-русски, и это сказано прямо в тексте. 4. НАБЛЮДЕНИЕ, важное для релиза: docs/{en,ru}/whats-new-1.8.md и docs/ru/sessions.md НЕ отслеживаются git. Сканеры доков читают только git-tracked файлы. Значит источник заметок к тегу НЕВИДИМ для всех doc-гейтов — зелёный гейт про него ничего не знает. Это подтверждает основание решения #225 измерением, а не рассуждением.
- 2026-08-03T13:45:21Z [implementation] — ВЕРИФИКАЦИЯ: AC-1: ✓ живая проверка + tests/test_doc_gate_list_parity.py (весь срез доков, 118 passed). Врезка 'три хранилища / три stores' добавлена в docs/en/shared-brain.md и docs/ru/shared-brain.md: таблица назначений (.tausik/tausik.db -> ~/.tausik-knowledge/knowledge.db -> Notion) и оба ломающих изменения названы номерами решений #221 и #222. AC-2: ✓ grep 'Classifier decides whether'/'Classifier решает' -> 0 вхождений. Заменено на 'классификатор риска': проверено по коду, scripts/brain_publish_flow.py:91 maybe_block_high_risk_publish действительно ОЦЕНИВАЕТ РИСК явной публикации, а не выбирает назначение. Классификатор не удалён из документа — он существует, у него сменилась роль; писать 'классификатора нет' было бы новой ложью. AC-3: ✓ grep 'brain mirror BLOCKED' по docs/ -> 0. Основание: та же строка отсутствует в исходниках scripts/ и harness/ (совпадает ТОЛЬКО устаревший scripts/__pycache__/service_knowledge.cpython-313.pyc), то есть доки описывали сообщение, которого код уже не может выдать. Подтверждено по scripts/service_decide.py:11-20 — 'It used to mirror decisions to Notion by itself... That is gone.' AC-4: ✓ ru и en правились парами в одном проходе, каждая правка применена к обоим зеркалам; расхождений по этим четырём файлам не осталось. AC-5 (негативный): ✓ сработал по-настоящему — найдено ещё четыре пункта того же класса, все занесены в журнал предыдущей записью с файлами и строками, ни один не починен молча. Один из них заставил меня исправить собственную формулировку AC1. Domain: результат осмыслен вне тестов — читатель, следующий докам, больше не ждёт автоматического зеркалирования в Notion, не ищет несуществующее предупреждение и не попадает из английской страницы в русскую документацию.
- 2026-08-03T13:45:54Z [implementation] — О ГРАНИЦЕ КВИТАНЦИИ, честно. Прогон #1672 упал: маппер не сопоставляет .md ни с одним тестом, и --no-tests-expected здесь был бы неправдой — тесты над деревом доков СУЩЕСТВУЮТ (test_audit_stale_docs, test_doc_drift_scanners, test_doc_gate_list_parity и другие), просто маппинг идёт от .py, а не от .md. Поэтому в relevant-files добавляю ИСПОЛНЯЕМУЮ поверхность проверки этих документов — scripts/docs_lint.py, scripts/doc_drift_scanners.py, scripts/audit_stale_docs.py. Это не расширение объёма правок: сами скрипты я не менял, они названы как то, чем правки проверяются. Квитанция покрывает прогон гейтов над docs/, а не изменение скриптов.
